# 更新记录

本文件记录每个版本的**用户可感知的变化**。内部重构、测试与文档的细节见
[`docs/HANDOVER.md`](docs/HANDOVER.md)（交接文档）与 git 提交历史。

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，**不带 `v` 前缀**。

---

## [0.1.7] — 2026-09-24

### 新增

- **图片管理面板**（命令「SyncHub：打开图片管理」、侧栏图片图标，或「图片同步」设置页里的按钮）。

  它把三件互相独立的事实摊平成一张可筛选的列表：**本地有哪些图**、
  **云端有哪些图**、**哪些图被笔记 / 画布 / Excalidraw 引用着**。
  三条轴可以任意组合筛选（有 / 无），于是常见的几类清理一次就能选出来：

  | 想做的事 | 筛法 |
  | --- | --- |
  | 把没传上去的补上 | 本地 = 有，云端 = 无 |
  | 清理失联图片（没人引用的） | 已链接 = 无，本地 = 有 |
  | 处理只留在云端的 | 本地 = 无 |
  | 压缩占体积的大图 | 最小体积 + 按体积排序 |

  列表里每行显示缩略图、路径、体积与三个状态徽标（「已链接」的徽标悬停能看到
  **是谁在引用它** —— 判断一张图能不能删时这是决定性信息）。

  批量操作：

  - **同步选中** —— 缺哪边补哪边（上传与下载都走这一条）。
  - **压缩并同步** —— 按设置页的质量 / 最长边重编码，**保持原格式**，
    而且**只在变小的时候才写回**（已经压过的图再压一次往往更大）。
  - **重命名** —— 模板式（`{name}` `{n}` `{ext}` `{date}`），带实时预览。
    会走 Obsidian 的官方改名入口，所以**笔记里的链接会跟着更新**；
    模板改了扩展名的那几条会被跳过（改名不改内容，换容器请在编辑弹窗里做）。
  - **删除本地** / **删除本地 + 云端** —— 前者会同时记下「这张别再同步回来」
    （否则镜像逻辑下一轮就把它下载回来，看起来像删除没生效）；
    后者会真的删掉云端那一份，弹窗里明确写了**云端没有回收站**。

  「云端」这一维读不到时（没配 R2、网络不通、桶权限不对）面板**照常可用**，
  只是会在顶部明确说明「云端这一列不可信」—— 不说的话「云端一张都没有」
  会被当真，进而让人放心地把本地全删了。

- **Excalidraw 画布里的图片引用也能被认出来**。Excalidraw 把画布数据用
  LZString 压缩后写进 `.excalidraw.md`，压缩之后图片的 `fileId` 在文件正文里
  **一个字都搜不到** —— 靠正则扫文本会得出「这张图没人引用」，于是清理失联
  图片会把它删掉。现在会解压场景再扫，并且**解压前先剥掉换行**
  （插件把 base64 每 256 个字符折一次行，不剥就解不开，而且不会报错）。

- **差异视图** —— 仓库同步面板上每个文件、每条提交都能点开看改了什么。

  - 入口有三处：面板里**每一行文件**（含冲突行）、**每条提交**、以及命令面板的
    「SyncHub：查看当前文件的差异」。三条路打开的是同一个弹窗。
  - 同一个文件的**工作区改动**与**已暂存改动**分开两节显示。`git status` 里的
    `MM` 意味着两边都有内容，只给一边等于把另一半藏起来 —— 而用户点开差异的
    意图恰恰是「我到底改了什么」。
  - 逐行给**新旧两个行号**，增删分别上色，`@@` 段落头保留（长 diff 里靠它定位）。
  - 未跟踪的新文件 git 不产出任何 diff，这里把内容读出来当成「全部新增」显示
    （库里新写的笔记正是最想看的那一类）；文件太大（>256 KB）则不读进内存，
    只说「文件太大」。二进制文件、纯重命名、内容被截断各自有明确说明 ——
    一片空白会被读成「没有改动」，而那是完全不同的结论。
  - 合并提交也能看：git 默认对合并提交不输出差异，这里按「与第一父提交比较」
    取内容，否则历史里点开一次拉取产生的合并提交只会是空白。
  - 中文与含空格的路径不会被转义成八进制（`\344\270\255`）—— 关掉了
    `core.quotePath`，否则库里的中文文件名在差异里读不出来。

- **「仓库同步」设置页里可以直接编辑 `.gitignore`**，不用再另开编辑器
  （也支持「在编辑器中打开」，两条路都留着）。

  多行代码框显示当前内容，配一个状态徽标（已保存 / 有未保存的修改 / 尚未创建）
  与三个按钮：**保存**、**填入默认内容**（只填进框里，不直接覆盖 —— 覆盖掉
  用户自己的规则是数据损失，要让他先看一眼）、**在编辑器中打开**。
  改动在**失焦时也会保存**，所以点到别处不会丢；文件不存在时**不会自动创建**
  （打开设置页不该往库里多出一个文件）。

  「在编辑器中打开」走不通时会**明说**（Obsidian 的库索引里不一定有以点开头的
  文件，那时点了会什么也不发生）并把用户指回这个框 —— 一个点了没反应的按钮
  比没有按钮更糟。

  把它放进设置页而不是只留一条命令：`.gitignore` 不是「一个顺带的文件」，
  它决定哪些文件根本不进版本控制（`workspace.json` 那种每次开关标签都变的
  文件，不同步它是避免多设备冲突的关键）。而此前唯一的入口在命令面板里 ——
  **只有已经知道有这个功能的人才找得到**。

### 变更

- **图片管理面板从弹窗改为主工作区的标签页**（命令「SyncHub：打开图片管理」、
   侧栏图片图标、「图片同步」设置页里的按钮，三条路都打开标签页）。

   整理图片时常常要**一边看着笔记一边决定哪张能删**，而弹窗把整个库盖住，
   只能二选一。标签页可以并排在笔记旁边。已经开着时再点入口只会把它显示出来，
   不会开出第二个（两个标签页扫的是同一批图，每扫一遍要好几秒）。列表高度
   **自适应页面**：表格吃满筛选区与操作栏之外的剩余高度、内部滚动，两块操作区
   始终可见。

- **「删除本地图片时，询问是否同时删除云端备份」从开关改成下拉**，三档可选：

  | 选项 | 行为 |
  | --- | --- |
  | 询问用户（默认） | 与原来的「开着」完全一致：弹一次窗，删 / 留由你定 |
  | 永远同步云端 | 一次都不问：删本地时直接连云端备份一起删（R2 没有回收站，删掉不可逆） |
  | 永不同步云端 | 与原来的「关掉」完全一致：云端永远不动 —— 但那一份会被下一次同步**下载回本地** |

  升级时旧开关的值会接过来（原来的「开」→ 询问用户，「关」→ 永不同步云端），
  **不改变任何人的现有行为**。

- **受管图片文件夹的默认值改为「仓库根目录」**（即整个库，`.`）。这个插件服务的
  场景就是「库本身是一个 git 仓库、图片要有云端副本」，而用户已经用 git 管着整个库 ——
  要更窄的范围（比如只同步 `attachments/`）改那一格即可。**已有安装不受影响**
  （`data.json` 里是什么就是什么），想用新默认的按一下「恢复默认」。

  「受管的图片文件夹」下面多了一行操作：

  - **浏览…** —— 列出库里的全部文件夹（可搜索，根目录排最前，已经在受管范围里的
    会标注）。手打路径的错法（`/attachments/`、`assets//img`、不存在的目录）
    全都没有提示，从现成的文件夹里挑一次性消掉它们。
  - **恢复默认** —— 一键回到仓库根目录；已经是默认值时按钮是灰的。

- **状态栏在没有状态可显示时改为显示本地化的面板名**（原来是写死的品牌名
  `SyncHub`）。英文界面下那句原来也是品牌名，现在跟着界面语言走。

- **`.gitignore` 模板按本库的配置目录名展开**，不再写死 `.obsidian`。
  配置目录是可以改名的（Obsidian 官方也支持），写死的话那几条排除规则
  **一条都匹配不上** —— 表现是「明明建了 `.gitignore`，`workspace.json`
  还是被同步出去了」，而用户完全看不出为什么。

### 修复

- 修好「图片管理改成标签页之后，列表里看不到缩略图」：Obsidian 的 app.css 里有
  一条 `.workspace-leaf-content img:not([width]) { max-width: 100% }`，**只对
  leaf 里的图片生效** —— 弹窗不在 leaf 里，所以以前从来没这个问题。表格是自动
  布局，路径那一列带着 `max-width: 0`（要靠省略号收尾），窄标签页里单元格会被
  挤到接近 0 宽，这条 max-width 于是让缩略图跟着缩到 0，整列消失。现在尺寸同时
  写成 HTML `width` / `height` 属性（选择器因此不再匹配），实际尺寸仍由 CSS 的
  em 值决定。

- 修好「填 `.`（整个库）」这个设置在**重新加载插件后丢失**的问题：归一函数把
  「整个库」的规范形状当成了空行丢掉，于是重启后变回「什么都没管」，
  「立即同步」还会报「还没有指定文件夹」。手改坏 `folders`（写成字符串 / 数字 /
  对象）时也一样 —— 它会被默认值兜底，而现在默认值是整个库，那等于一次手滑
  变成「同步全部」；现在坏形状一律退到空列表并提示。

- 修好「删掉本地图片后选择保留云端备份」这条路径（`keepRemoteBackup` 被
  `markLocalDeleted` 取代之后调用方没跟上，9 条用例因此是红的）。
  顺带把墓碑的记账时机提前到**询问之前**：攒批的几百毫秒里如果正好有一轮
  同步在跑，它会把用户刚删掉的图当作「云端有、本地没有」补齐回来。

- 修好差异视图**少显示一行**的问题：内容本身以 `-- ` 开头的行，删掉之后产出的是
  `--- xxx` —— 与 unified diff 的文件头**长得一模一样**，按行首判断就会被当成
  段落头吞掉（表现为界面上少一行、行号与文件对不上）。现在按「是否已经进入 hunk」
  判断，并有一条用例专门钉住这个形态（去掉守卫后只有它会红）。

- 修好「`.gitignore` 编辑框只占右侧一小块」：它原来是设置项的**控件**，
  而 Obsidian 的控件区是给「一个下拉 / 一个输入框」留的宽度（几百像素），
  十几行规则挤在里面根本没法读。现在它是直接挂在页面上的**全宽块级代码框**。

- 「在编辑器中打开 .gitignore」打不开时会**明说**（Obsidian 的库索引里不一定有
  以点开头的文件，那时点了什么也不会发生），并把用户指回设置页那个能直接改的框 ——
  一个点了没反应的按钮比没有按钮更糟。

### 内部

- **按社区审核规范（`eslint-plugin-obsidianmd`）做了一轮复查**，修掉 17 处：
  静态导入 `node:os`（改成 `Platform.isDesktop` 守卫内的 `require`）、
  `console.info`（审核的 `no-console` 只放行 debug / warn / error）、
  `setTimeout` / `clearTimeout` 未走 `window`（弹出窗口下行为不一致）、
  `document.createElement`（改用 `createDiv` / `createEl`）、
  写死的配置目录路径，以及**直接写内联样式**（`el.style.width = "…"` ——
  审核把它算作 error，`setCssProps({ width })` 同样不行：那条规则只放行
  `--*` 自定义属性，静态宽度必须待在 CSS 类里）。
  剩 1 条 `settings-tab/prefer-setting-definitions` 是**有意保留**的：
  设置页整体是手写渲染的，迁到 1.13 的声明式 API 是一次独立的改造。

---

## [0.1.6] — 2026-09-21

### 修复

- **插件 id 回退为 `ob-sync`**（0.1.5 里改成了 `synchub`）—— 社区审核报
  *The plugin ID in (manifest.json) does not match the existing plugin ID*：
  **审核系统在 0.1.4 那次提交时就把本插件登记为 `ob-sync`**，之后改 id 就与既有登记不匹配。

  显示名 **SyncHub 保持不变**（商标那条已经过了，本次审核没有再报名字问题）。
  所以最终形态是：**显示名 `SyncHub` + id `ob-sync`** —— 两者不一致是**有意的**：
  审核登记决定 id，商标规则决定显示名，来源不同，不必也不该统一。

  **升级注意（如果你装过 0.1.5）**：安装目录会从
  `.obsidian/plugins/synchub/` 回到 `.obsidian/plugins/ob-sync/`，
  请把 `data.json` 复制回去（否则会以默认设置启动），并删掉 `synchub/` 目录。

  **访问令牌始终不受影响**：它们存在 `obsync-token-*` 这个密钥 id 下，
  与插件 id 无关 —— 这一来一回都没有作废过凭据。

[0.1.6]: https://github.com/Dyse-Sofqi/SyncHub/releases/tag/0.1.6

---

## [0.1.5] — 2026-09-21

### 变更

- **插件名从 `OBSync` 改为 `SyncHub`** —— 社区审核以
  *Plugin name must not include parts of the name "Obsidian"* 打回。
  原因不是"含 Obsidian 这个词"（`OBSync` 并不含），而是它以**全大写 `OBS`** 开头 ——
  那正是 "Obsidian" 前三字母的缩写写法，规则查的是商标的**片段**。

  实测佐证：插件目录 7884 个已上架插件里，**没有任何一个**显示名含全大写 `OBS`；
  而含 `Obs` / `obs` / `Ob` 的各种写法都大量在架（`WeChat Obsync`、`Tree Obs`、
   `ObShare`、`GitHobs`…）。

  改动范围：`manifest.json` 的 `name`、状态栏文案、命令名、i18n 文案、README，
  以及**自我更新的仓库坐标**（GitHub / Gitee 仓库同步改名为 `SyncHub`）。

  **那些 `obsync-` 前缀的内部标识一律不动** —— CSS 类名、`obsync-sync-view`
  视图类型、`obsync-token-` 密钥 id 各有各的命名空间，跟着改只会白白作废用户
  已存的令牌与面板状态。

  **升级注意**：仓库改名后 GitHub 会自动重定向旧地址，自我更新不受影响。
  但若你把设置页的「自身更新来源」手填成了 `.../OBSync`，需要改成 `.../SyncHub`。

- **插件 id 从 `ob-sync` 改为 `synchub`** —— 与显示名统一。
  **这不是审核要求的**：商标规则只管显示名，目录里 `obsync-ptop`、
  `obsync-webdav-gpg` 等一大批 id 含 `obsync` 照旧在架。改它是因为**现在最便宜** ——
  插件尚未上架，全世界的安装只有作者自己的库，迁移成本只有那几处；
  等上架之后再改，代价就是所有用户的数据。

  **升级注意（重要）**：Obsidian 按 id 找插件，安装目录会从
  `.obsidian/plugins/ob-sync/` 变成 `.obsidian/plugins/synchub/`，
  而 `data.json`（设置 + 跟踪列表）**不会自动跟着走** —— 需要手动把它复制到新目录，
  否则插件会以默认设置启动。旧的 `ob-sync/` 目录可以删掉，
  否则插件列表里可能出现两份。

  **已存的访问令牌不受影响**：它们存在 `obsync-token-*` 这个密钥 id 下，
  与插件 id 无关（这正是内部前缀刻意不改的价值）。

  **跨 id 这一跳不能靠自我更新**：旧安装里的自我更新会校验远端 manifest 的 id，
  而远端已是 `synchub`，校验不通过就会**拒绝写盘**（这道守卫本就是为了防止
  覆盖别的插件）。所以需要**手动重装**一次；重装后两边 id 一致，自我更新恢复正常。

### 修复

- **英文界面的 `.gitignore` 模板漏了排除本插件自己的 `data.json`** ——
  中文模板一直有这条（本插件的设置是按设备的，同步它只会让两台设备互相覆盖设置），
  英文模板却**没有**，于是英文用户初始化仓库时写出的 `.gitignore` 不会排除它。
  i18n 的编译期检查只管**键结构**、不管字符串内容，所以这条漂移长期没被发现。
  已补齐，中英模板现在一致。

[0.1.5]: https://github.com/Dyse-Sofqi/SyncHub/releases/tag/0.1.5

---

## [0.1.4] — 2026-09-20

### 变更

- **插件 id 从 `obsync` 改为 `ob-sync`** —— 插件市场里 `obsync` 已被别的插件占用，
  id 撞车的插件无法上架。插件名仍是 **OBSync**，功能与设置项一个没少。

  只动了「插件身份」这一处：`manifest.json` 的 `id`、自我更新时校验远端身份用的
  常量，以及它们派生出来的东西（`.gitignore` 模板里的设置文件路径、安装目录）。
  那些 `obsync-` 前缀的内部标识**刻意不动** —— CSS 类名、`obsync-sync-view` 视图
  类型、`obsync-token-` 密钥 id 各有各的命名空间，跟着改只会白白作废用户已存的
  令牌与面板状态。

  **升级注意**：Obsidian 按 id 找插件，所以目录会从
  `.obsidian/plugins/obsync/` 变成 `.obsidian/plugins/ob-sync/`。旧目录要手动删掉，
  否则库里会同时存在两份（同一个名字、两个 id），插件列表里会出现两个 OBSync。
  旧的 `data.json`（设置与跟踪列表）不会自动搬过去，需要重新配置。

[0.1.4]: https://github.com/Dyse-Sofqi/SyncHub/releases/tag/0.1.4

---

## [0.1.3] — 2026-09-20

### 新增

- **可以指定自身更新的来源** —— 设置页「OBSync 自身」一节新增「自身更新来源」。
  留空走官方仓库；国内访问 GitHub 慢或被阻断时，填 Gitee 镜像的地址
  （例如 `https://gitee.com/sofqi/OBSync`），**填一次就一直用它，不再自动探测**。
  检查与更新用的是**同一个来源**（不会出现「检查说没有更新、更新却从另一个仓库拉」）；
  地址非法时明确报错，而不是悄悄回退到官方。更新前仍然校验远端 manifest 的 `id`，
  所以地址填错不会覆盖别的插件。

  在这之前，想从镜像更新自己只能绕道「添加插件仓库」把自己加进跟踪列表 ——
  而那条路会**先禁用正在运行的这个插件再重新启用**，中途失败就停在「已禁用」，
  重启也回不来。所以这个入口不只是方便，它把一件本来只能危险地做成的事变安全了。

### 内部

- 修掉一处「跨 host 传参会丢 host」的隐患：`formatRepoId` 只给 `owner/repo`，
  跨 host 传递时必须另传 `defaultHost`，否则 Gitee 的坐标会被按 GitHub 解析 ——
  而界面看起来毫无异常

[0.1.3]: https://github.com/Dyse-Sofqi/SyncHub/releases/tag/0.1.3

---

## [0.1.2] — 2026-09-20

侧边栏的同步面板从「一个标题 + 四个按钮 + 一串文件名」变成了**真正的 git 详情面板**；
插件安装补上了**回退到指定版本**的能力；另外修掉几个会把人指错方向的报错。

### 新增

**笔记仓库同步**

- **仓库同步视图**（侧边栏）：侧栏的 git 图标、点状态栏条目，或命令
  **OBSync：打开仓库同步面板**。里面有：顶部**一行**工具条（提交 / 拉取 / 推送 /
  分支下拉 / **靠右的**立即同步 / **最右的**刷新）、远端地址（**脱敏**后回显）+ 编辑入口、
  `领先 / 落后远端`、**并排两栏**的仓库大小与待提交改动、冲突区（列出冲突文件 +
  放弃合并）、按「已暂存 / 更改」分组的文件列表（逐个文件暂存 / 取消暂存、
  点文件名打开笔记、在远端打开此文件）、最近 10 条提交（点 hash 在远端查看）。
  不是 git 仓库时这里直接给「初始化仓库」按钮。面板是**活的**：自动提交、库外改动、
  命令面板里的动作都会让它自己刷新
- **展示仓库体积**（松散 + pack 对象 + 对象数）与**待提交改动体积**。
  **读不到时说「读不到」，不显示 `0 B`** —— 0 B 会被当成「空仓库」，那是个错误结论
- **「与远端一致」的醒目反馈**：提交/同步结束且本地与远端完全一致时，给一条带 ✓、
  停留更久的提示（附仓库大小），同时状态栏出现 `✓`、面板里那一行转绿。
  三处用的是**同一个判据**。关掉「显示操作结果提示」的人看不到提示，
  但状态栏与面板仍然显示这个状态
- **「立即同步」为什么包含拉取**写进了 README：git 的 `push` 只能**快进**，远端有你
  没有的提交时会被直接拒绝 —— 而这个插件的用途就是多设备同步，所以「提交 → 推送」
  在两台设备上是**稳定失败**，不是偶发失败

**社区插件与主题**

- **切换版本（回退）**：跟踪列表里新增「版本管理」按钮（**只有插件有** —— 主题在设计上
  就没有版本钉选）。弹窗列出该仓库已发布的版本、标出「当前」那一版、**默认选中你之前
  钉住的那一版**（而不是最新），选完即切过去。列表上会挂「已固定 \<tag\>」徽标
- **进度看得见**：长耗时动作的图标换成会转的 `loader`，并给带圆环的逐文件进度提示
  （「Trefoil：正在获取 main.js…」）
- **换下载来源**：版本弹窗里可探测并改用 Gitee 镜像；探不到时也能**手填**镜像地址
  （用 manifest 的 `id` 校验，不一致直接拒绝）
- **疑似镜像必须由你确认才采用**：确认页把两个地址做成**可点开的链接**，方便你去核对

**设置页**

- 「通用」标签新增**状态栏占满整屏宽**（默认开着）。同步条目要贴在状态栏最左侧，
  而状态栏原本收缩在右下 —— 左边没有空位，唯一办法是把它拉成全屏宽
- 「仓库同步」标题下新增**注意事项**（见下方 `reset` 那一条）

### 变更

- **视图改名**：「源码控制」→「仓库同步」。面板内那个重复的标题删掉了 ——
  标签页上已经写着视图名。命令名、状态栏悬停文案、侧栏图标标题一并跟上
- **工具条从三行并成一行**（原来标题一行、动作一行、分支一行）
- **删掉「重装」按钮**：它与「版本管理」做的事**逐字相同**（同一个 `install()` 调用、
  同一组参数），留着只是让人猜该点哪个。圆箭头图标还给「检查更新」（本来就是它的）
- **「提交全部」改名「提交」**（行为不变，仍是暂存全部 + 提交；命令面板里那条仍叫
  「提交全部更改」—— 那里空间够）
- **`reset` 策略与自动同步互斥**：选「重置」作为拉取整合策略时，**自动同步会被暂停**
  （开关灰掉并说明原因）。因为同步链路是「提交 → 拉取 → 推送」，而重置会把刚提交的
  内容丢掉 —— 两者叠在一起，每 N 分钟就**静默地**丢一次。改回「合并」或「变基」后
  自动恢复，不用重新拨开关

### 修复

- **「推送」与「提交」的区别没写在界面上** —— 用户是问了才知道的，说明没写清楚。
  现在三个动作按钮都有悬停提示，且推送时按状态各说一句。其中最关键的一条：
  有未提交改动时说**「你有 N 个改动还没提交」** —— 原来这种情况说成「没有需要推送的
  内容」，听起来像「一切正常」，而真实情况是「你的改动一个都没上去」
- **「一直看到正在推送」** —— 状态栏的活动态**没有终点**：`setActivity("idle")` 这个
  调用在 `src` 里从未出现过，于是动作结束后状态栏永远停在「正在推送…」，
  连分支和 ahead/behind 都不再显示，直到重载插件
- **git 可能真的在等人回答** —— 拿不到凭据时 git 会提问，而 Obsidian 里没人能回答，
  命令挂着、后面所有同步排队等它。现在禁掉「问人」（**不禁「用凭据」**，已存的照常用），
  并给一个**无输出超时**（120 秒，传输有输出就重置）
- **从 Gitee 镜像装插件的资产全 404** —— Gitee 的资产 JSON 里**没有 `id` 字段**，
  而代码写的是 `String(asset.id)`：缺字段时得到字符串 `"undefined"`，**它是真值**，
  于是拼出 `.../attach_files/undefined/download`。症状只出现在**配了 Gitee 令牌**的人
  身上（那条分支要 token 才进）。现在缺字段就保持缺席，附件端点失败时回落到公开下载地址
- **「装不上、报缺 main.js」** —— 资产通道的「短路」写宽了作用范围：第一个文件传输
  失败后，后续文件被**跳过资产通道**只去源码里找，而 `main.js` 是构建产物、仓库里没有，
  于是「源码里找不到」被当成「这个插件缺 main.js」。同一处还让 `styles.css` 被静默跳过
  ——**能装出一个没有样式的插件**，界面是坏的且没有任何提示
- **「更新完还报可更新」的死循环** —— 记录被抹成了空版本
- **「插件版本被退回而 OBSync 还说已是最新」** —— 让记录与磁盘对上，并优先听 Obsidian
- **镜像发现只探「同名 owner」** —— 挂在作者自己 Gitee 账号下的镜像永远找不到
- **`.gitignore` 模板漏了 `.obsidian/plugins/obsync/data.json`** —— 初始化仓库时建的
  `.gitignore` 里缺这条，而它是**按设备**的设置，跨设备同步只会互相覆盖

### 内部

- **`pnpm verify:head`**：在 **HEAD**（而不是工作区）上跑测试。`pnpm test` 读的是工作区，
  所以「本地全绿」说明不了 HEAD 是绿的 —— 而 HEAD 才是别人克隆时看到的
- **`pre-push` hook**（`pnpm hooks:install` 启用）：push 前自动跑一次上面的检查，
  HEAD 红了就拦住
- 838 条单元测试（55 个文件）
- 交接文档新增五点七（版本管理）与五点八（仓库同步视图）两节

[0.1.2]: https://github.com/Dyse-Sofqi/SyncHub/releases/tag/0.1.2

---

## [0.1.1] — 2026-09-18

首个公开发布版。从零搭起了两件事：**用 git 同步笔记仓库**，以及**安装社区插件与主题**，
并且把两者都从「仅 GitHub」扩展到 **GitHub / Gitee 双平台**，界面中文优先、英文对等。

### 新增

**笔记仓库同步**（仅桌面端 —— 依赖系统 git）

- **一条链走完**：`提交 → 拉取 → 推送`（「立即同步」），也可以单独执行提交 / 拉取 / 推送
- **冲突不替你决定**：检测到冲突时在库根目录写一份《OBSync 冲突指南.md》列出冲突文件，
  并**立即停止同步链**（继续提交会把冲突标记写进历史，继续推送会把它们推上远端）；
  手动解决后重新同步，或用「放弃当前合并」回到拉取之前
- **初始化仓库**时顺手建一份 `.gitignore`（默认排除 `.obsidian/workspace.json` 这类
  每台设备各自维护的文件）；**已存在的 `.gitignore` 绝不覆盖**，另有「编辑 .gitignore」命令
- **自动同步定时器**：自动提交 / 自动推送 / 自动拉取三个间隔（分钟，0 = 关闭）。
  计时基于**上次执行时间**并存进库级存储，重启 Obsidian 不重置周期、多个库互不干扰
- **源码控制视图**（侧边栏）：分支、ahead/behind、脏文件与冲突列表，逐个文件暂存 / 取消暂存
- **状态栏条目**：分支 / `↑ahead ↓behind` / `~脏文件数` / `⚠冲突数` 与进行中的动作
- **在浏览器中打开**：当前文件 / 当前文件的历史 / 文件右键菜单 —— GitHub 与 Gitee 各按平台拼链接
- **连接测试（诊断）**：git 可用 → 已初始化 → 有远端 → 平台可识别 → **真的连一次远端**，
  逐条给出结果；并明确说明它**只验证读取**（推送权限要在真正推送时才知道）
- **编辑远端地址**：带凭据（`https://user:token@…`）的地址会给出警告，提示信息同样脱敏

**社区插件与主题**

- **从地址安装**：填 `owner/repo` 简写或粘贴完整链接，GitHub / Gitee 都能识别
  （识别结果会显示是哪个平台，命中镜像时另有一行说明）
- **两条通道**：优先 release 资产（`main.js` / `manifest.json` / `styles.css`），
  仓库没有发 release 时**回退到源码文件** —— Gitee 上大多数插件仓库没有 release，
  这条通道是必需的
- **版本选择**：默认最新，可从 release 列表里挑具体版本；只有预发布版的仓库会回退到预发布版
- **写入前备份 + 失败整体回滚**：安装失败不会在库里留下半个插件
- **更新检查**：单个检查 / 全部检查 / 启动后延迟检查 / 进入设置页时检查（可关），
  有更新的行常驻徽标；**只检查并提示，安装永远手动**
- **跟踪列表**：重装、冻结（不参与更新检查）、打开仓库页、取消绑定（**只移出列表，不动文件**）
- **绑定库里已装的插件与主题**：按 manifest id / 主题目录名反查官方社区索引，一次性纳入跟踪；
  来源识别不出来的可以手填仓库地址
- **Gitee 镜像发现**（默认关闭）：安装 GitHub 插件时探测 Gitee 上的同名镜像，
  用两边 manifest 的 `id` 二次校验，命中则改用镜像下载；命中后列表会**同时显示源仓库与镜像**
- **OBSync 自身更新**：设置页里检查并更新 OBSync 自己（写入新版本文件，重启 Obsidian 生效）

**平台与体验**

- **双平台适配层**：GitHub 与 Gitee 的差异（鉴权方式、release 排序、raw 通道、限流特性）
  只实现一次
- **访问令牌存进系统密钥库**，不写进 `data.json`、不随库同步；错误提示与日志里的令牌一律脱敏
- **中文优先、英文对等**：所有界面文案与错误信息都走 i18n（错误只携带类型码与参数，文案集中在 locale）
- **移动端可加载**：插件安装是纯网络操作，移动端可用；同步模块在移动端不加载

### 修复

- **装完新插件后列表不刷新** —— 要手动点一次「检查全部更新」才会出现；现在安装成功即重绘列表
- **状态栏条目不在最左侧** —— 条目原先排在状态栏簇的最右（且会把其他插件的条目挤走一个条目的宽度）；
  现在贴在状态栏**最左**，且**一个别的条目都不动**（只靠 CSS 的 `order` + `margin-right`，不碰 DOM 顺序）
- **更新/重装/安装后的提示不说明来源** —— 现在每次都报「来源：GitHub」或「来源：Gitee 镜像」，
  用户不必再猜到底走没走镜像
- **「识别」按钮永远是灰的** —— 可用性只在渲染时算过一次，输入地址后不更新
- **库根路径被 `normalizePath` 剥成相对路径** —— 在 macOS / Linux 上会导致同步模块连不上仓库
  （Windows 上表现为正常，所以一直没被发现）
- **状态栏文案定格在启动语言** —— 切换语言后不跟着变
- **「启用笔记同步」是个死开关** —— 关掉之后自动提交照样把笔记推上远端
- **更新检查永远报「已是最新」** —— 检查路径比安装路径少做了两件事（不带令牌、不回退到预发布版），
  私有仓库与「只发预发布版」的仓库因此永远收不到更新提示
- **`data.json` 里的 `pluginId` 未校验内容** —— 手改或随库同步过来的异常值会让卸载删到插件目录之外
- **取消绑定时不删文件** —— 早期实现会连插件目录一起删；现在只移出跟踪列表
- **冲突标记会被提交进历史** —— 冲突文件在 `git status` 里同时算暂存与未暂存，导致「有改动」判断放行
- **同一个文件被重复计入三处** —— 改了又暂存（`AM` / `MM`）的文件在文件数、列表与状态栏里各被算两次
- **视图里点按钮失败时界面毫无反应** —— 推送被拒、鉴权失败、git 缺失等错误被静默吞掉
- **Gitee 的令牌会随错误消息漏出去** —— 屏幕上与控制台日志里都会带出完整令牌（已全面脱敏）
- **Gitee 私有仓库必然被拒** —— git 的 Basic 认证用户名填成了 `git`（GitHub 的习惯写法），
  而 Gitee 只接受账号名 / `oauth2` / `gitee.com`
- **「用户名被拒」被当成「令牌失效」** —— 让用户去反复检查一个没问题的令牌
- **插件目录名 ≠ manifest id** —— 目录名只用于定位文件，插件身份一律用 manifest id（实测测试库 32 个插件里 5 个错位）
- **移动端启用即崩** —— 同步模块的静态导入会把 `simple-git` 拉进来（模块初始化阶段就 `require` Node 内置模块）
- **自动定时器的两个真 bug** —— 存储未按库隔离（A 库影响 B 库计时）、`stop()` 挡不住在途回调导致同一动作每周期跑两次
- **资产 404 被当成「资产通道整体不可用」** —— 一次本可成功的安装变成失败
- **网络错误里的尝试次数不实** —— 日志写「重试 3 次」而实际只发了 1 次
- **「编辑远端」只接受 GitHub / Gitee 地址** —— 同步是纯 git 操作，自建 GitLab / 内网 git / 本地裸仓库也能用
- **「放弃当前合并」后毫无反馈**
- **设置页「自动提交间隔」少写了「并同步」** —— 到点执行的是完整链路，文案会让人以为只提交
- **`minAppVersion` 写低了**（1.5.0 → 1.8.7）—— 低版本 Obsidian 装上会崩
- **22 处错误文案硬编码中文** —— 英文界面下会冒出中文
- **7 个 i18n 键写了却没接上**

### 内部

- 项目自查脚本 `pnpm check`（六项：`minAppVersion` 一致性、硬编码中文、死键、
  CSS 类覆盖、移动端安全、设置项无人读取）
- 634 项单元测试（含装配路径冒烟、真实 git 进程、镜像来源、i18n 键覆盖）
- 交接文档 [`docs/HANDOVER.md`](docs/HANDOVER.md)、参考项目分析
  [`docs/reference-analysis.md`](docs/reference-analysis.md)、发版清单 [`docs/RELEASE.md`](docs/RELEASE.md)

[0.1.1]: https://github.com/Dyse-Sofqi/SyncHub/releases/tag/0.1.1
