# 2026-09-19

> 接 `2026-09-18.md`。中间隔了一轮（另一批工作已提交，见 `git log`：疑似镜像确认、
> 记录与磁盘版本对齐、主题也能用镜像）。本轮做的是**新功能**：已跟踪插件的版本管理。

---

## 轮次：`requestedVersion` 一直存在，而界面上没有它的入口

### 问题

`requestedVersion`（「用户要求的版本」）是这个项目里一个**写了却没有入口**的字段：

- `install()` 每次都把它写进记录（`recordItem`），`reinstall()` 读它（重装装回那一版）；
- 而界面上**只有**「添加插件仓库」弹窗写过它一次 —— 装完之后既改不了、也**看不见**。

后果很具体：一个装坏了的版本，除了等作者发新版没有别的出路。而「退回上一版」恰恰是
遇到坏版本时最该能做的事（参考项目 BRAT 有 frozen-version，就是这个用途）。

这和 `sync.enabled`（那个「死开关」）是同一类问题，只是方向相反：
那里是**开关有效果但没人读**，这里是**字段有效果但没人写、也没人显示**。

### 做法：给跟踪列表加第七个按钮，只有插件有

- 新弹窗 `ui/VersionManagerModal.ts`：列出该插件已发布的版本，选一个切换过去。
  三条刻意的设计 ——
  1. **认得出当前版本**：磁盘上装的那一版标「当前」（`1.2.3 · 当前`）。一串 tag 里
     认不出自己在哪一版，「退回上一版」这个动作就无从下手；
  2. **默认选中记录里的选择**（不是「最新版本」）。默认成「最新」会把这个控件变成一个
     不断把用户往最新版拽的东西，而他要确认的是「我钉在哪一版」；
  3. **「没有版本可切」与「这次没拉到」分开说**。前者是这个仓库的事实（Gitee 上
     「只有源码、不发 release」是常态），后者是请求失败、可以重试。混成一句，
     用户会去重试一件永远不会成的事。
- 执行走 `install()`（**不是** `reinstall()`）：要装的正是用户刚选的那个 tag，
  而 `install` 会把它写回记录 —— 也就是**钉在这一版**。于是三条现成语义一起成立：
  重装装回它；「更新到最新版本」/「更新全部」把记录改回 `latest`（解开钉住）。
- **列表上加「已固定 \<tag\>」徽标**：这个状态只在 `data.json` 里，而它的后果是
  「重装会装回旧版」。不显示的话，用户看到旧版本号分不清是自己选的还是更新失败留下的。
  徽标措辞刻意与「已冻结」区分：冻结 = 不参与检查，钉住 = 认准这一版但**照常检查**。

### 只有插件有（刻意的不对称）

主题在设计上就没有版本钉选（`TrackedTheme` 上没有 `requestedVersion`，
`updateTheme()` 永远按最新走）。给主题一个能选版本的按钮，等于承诺一件做不到的事，
所以它不出现 —— 用例把这条不对称钉住了（插件 7 个按钮、主题 6 个）。

> 要给主题也加，得先给它一个「钉住」的概念（字段 + `updateTheme` 收版本参数）。
> 那是**产品决定**，不是顺手能实现的细节，所以本轮没做。

### 一个容易写错的判据

**「当前」标记必须用版本比较，不能用字符串相等**：release tag 常带 `v` 前缀
（`v1.2.3`），而 manifest 里的 version 不带（`1.2.3`）—— 字符串相等时
「当前」两个字永远不会出现，而用户到这里来正是为了认清「我在哪一版」。
改用 `compareVersions`（`semver.coerce` 归一，与更新检查同一判据），
非 semver 的 tag 才退化成相等比较。

### 变异验证（四个方向，全部被抓住）

| 变异 | 被哪条用例抓住 |
| --- | --- |
| `switchVersion` 改传 `"latest"`（等于「回退」永远失效） | 「按那个 tag 安装」 |
| 「当前」标记退回字符串相等（`v1.2.0` 认不出） | 「不同形时照样认得出」 |
| 去掉「已固定」徽标（状态重新变成不可见） | 「挂已固定徽标」 |
| 让主题也拿到那个按钮（承诺做不到的事） | 两处不对称用例同时失败 |

新增 17 条用例：`versionManagerModal.test.ts` 13 条（列表状态 / 当前版本标记 /
默认选中与提交），`trackedItemsList.test.ts` 4 条（接线 / 执行 / 徽标 / 主题不对称）。

### 记一条方法上的事

这轮修的是「**字段有效果、界面没有入口**」这一类洞 —— 它和上一批修过的
「开关有效果、没人读」是镜像关系。两者都**不是**类型或测试能自动发现的：
类型上 `requestedVersion: string` 完全合法；测试里没人会去点一个不存在的按钮。
`scripts/checks.mjs` 的第 6 项（设置项无人读取）只覆盖后一个方向，
前一个方向目前只能靠**逐个字段问「它从哪写、在哪显示」**。

验证：`pnpm check` 6 项全通过；`pnpm test` 全绿；`pnpm build` 通过并部署到两个库
（`F:/_Workspace/Plugin-Test` 与 `D:/_Workspace/learning-records`）。

---

## 轮次：真机上装不上 Trefoil —— 报的是 `missingRequiredFiles: main.js`

用户贴来一段控制台日志（他刚装上新版、去点插件行上的按钮）：

```
[OBSync] operation failed installer error [missingRequiredFiles]
{"repo":"Dyse-Sofqi/Trefoil","files":"main.js","of":"plugin"}
```

问「为什么会这样」。查下来是**两个真 bug 叠在一起**，而且第二个是「报错指错方向」。

### 先确认事实（不要凭感觉）

```
gh api repos/Dyse-Sofqi/Trefoil/releases  → 1.0.0~1.0.3，每个都带 main.js/manifest.json/styles.css
仓库 tree（HEAD / 1.0.3 / 1.0.0）        → 没有 main.js，也没有 styles.css（都是构建产物）
本机下载同一个资产四次                   → 25s 超时 / 16.8s 成功 / 0.6s / 0.6s（raw 域名同一时刻 485ms）
```

也就是说：**文件确实在 release 里，只是这一次没下下来**。而 http 层的超时阈值是
20 秒 —— 16.8 秒那种「成功」就压在边界上，所以同一条 URL 一会儿成一会儿败。

### bug 1：那条「短路」把一次本可成功的安装变成了失败

`fetchFromRelease` 里有一条优化：某个文件的资产因传输层失败后，**后续文件不再试资产**
（为的是别白等三次超时）。但它的作用范围写宽了：

1. `manifest.json`（第一个文件）的资产在冷连接上超时 → 回退源码成功 → 记住「资产不通」；
2. `main.js` 于是被**跳过资产通道**，只去源码里找；
3. 而它是构建产物，仓库里没有 —— 于是「源码里找不到」被当成「这个插件缺 main.js」。

而按实测，那个被跳过的资产**重试一次只要 0.6 秒**。所以现在：**任何文件在源码里也
拿不到时，为它单独重试一次资产通道**（资产干净地 404 时不重试 —— 那是远端确实没有）。
代价最多一次超时，而那条路径本来就要失败。

顺带发现同一处还有第二个受害者：`styles.css` 也被跳过（Trefoil 同样是构建产物），
**原来会静默装出一个没有样式的插件** —— 插件能跑、界面是坏的，而且没有任何提示。
同一处修复一并覆盖。

### bug 2：报「找不到 main.js」，把用户指去检查一个没问题的地方

原来的报错只有一种 `missingRequiredFiles`（「找不到 X，无法安装该插件」）。可是
「缺文件」有两种完全不同的成因，下一步动作也完全不同：

| 成因 | 用户该做什么 |
| --- | --- |
| 仓库/资产里确实没有它 | 去看作者的发布流程（或自己补发） |
| 挂在那儿但没下下来 | 检查网络、重试、换 Gitee 镜像 |

现在用 release 的资产名判定，第二种报 `assetDownloadFailed`（新类型码 + 中英文案）。
顺带发现 `missingBuildArtifacts` 这个键**写了却从来没抛出过**（死键，只是被
`describeInstallerError` 引用所以自查扫不到）—— 留着，见「保留但未接线」那套先例。

### 变异验证（两个方向都被抓住）

| 变异 | 被哪条用例抓住 |
| --- | --- |
| 去掉「资产 404 就不重试」的判断 | 「资产 404 时不为它重试」 |
| 去掉 `assetDownloadFailed` 分类 | 「资产挂着却没下下来」+「资产 404 时不为它重试」 |

新增 4 条用例（`installFiles.test.ts` 19 条）。**一条老用例按新规则改了期望**：
「资产通道网络不可达时，后续文件不再试它」—— 短路现在只对「源码里拿得到的文件」生效，
所以 `downloads` 里多了 `styles.css`（它只有资产里才有）。这条改动是**有意的行为修正**，
不是为了让测试变绿。

### 一句提醒（写给下次的自己）

`missingRequiredFiles: main.js` **不等于**「作者没上传 main.js」。先看 release 里有没有它，
再决定是查网络还是查发布流程 —— 我这次差点顺着「构建产物没提交」这个思路查错方向，
是先去读了真实仓库的资产列表才拐回来。

验证：`pnpm check` 6 项全通过；`pnpm test` **697 项全绿**（51 个文件）；`pnpm build` + 部署。

---

## 轮次：两件用户直接提的事 —— 「显示加载动画」与「为什么它不走 Gitee」

用户的第二、三句话（接着上一次的失败）：

> 获取插件时，请显示加载动画，不然我根本不知道你是不是在更新。另外 Trefoil 插件我选了
> 1.0.2 旧版安装，但是他却不走 gitee 路线，也找不到选择 gitee 镜像下载的选择

### 先查事实：Trefoil 到底有没有 Gitee 镜像

有，而且**比 GitHub 快一个数量级**（同一时刻实测同一个 `main.js`）：

| 来源 | 实测 |
| --- | --- |
| `github.com/Dyse-Sofqi/Trefoil/releases/download/1.0.3/main.js` | 25 秒超时 ／ 16.8 秒 ／ 0.6 秒 ／ 0.6 秒（963 KB） |
| `gitee.com/sofqi/Trefoil/releases/download/1.0.2/main.js` | **200，3.1 秒**（939 KB） |

`gitee.com/sofqi/Trefoil` 有 1.0.2 与 1.0.3 两个 release，资产齐全
（`main.js` / `manifest.json` / `styles.css` + zip + tar.gz）。而 `gitee.com/Dyse-Sofqi/Trefoil`
**404** —— 这正是探测失败的原因：

自动探测只猜两个候选（`findGiteeMirror` 的 `mirrorOwnerCandidates`）：
1. **同名 owner**（`Dyse-Sofqi`）→ Gitee 上没有 → 不成立；
2. **你 Gitee 账号名下的同名仓库**（`sofqi`）→ 但账号名要调 `GET /v5/user` 解析，
   **那需要 Gitee 令牌**。

用户没填 Gitee 令牌时，两个候选都不成立 → 探测永远找不到它。**而界面上没有任何入口**
能把这第三个地址指出来 —— 这就是「找不到选择 gitee 镜像下载的选择」的由来。
（跟踪列表里那条「疑似镜像 · 尚未使用，待确认」也帮不上：提议只在**安装成功之后**才写入，
而那次安装正好失败了。）

### 做法

1. **「版本管理」弹窗里加一节「下载来源」**：开弹窗时跑一次探测（`probeMirror`，只提议），
   探到就给「改用 Gitee 镜像」按钮；**探不到也要解释为什么**（只猜两个候选，别的账号下就手填）。
2. **手填镜像地址**（`InstallerService.setMirror`）：自动探测的兜底。校验用 manifest 的 `id`
   —— 与自动探测同一条判据，**不一致直接拒绝**（`mirrorIdMismatch`），因为只比仓库名会装错东西。
   确认之后走既有的 `confirmMirror`：主来源换成它、原来源进 `origin`。
3. **换完来源必须重拉版本列表**：列表是从当前来源查的，换了仓库 tag 可能完全不同
   （Trefoil 的 Gitee 只有 1.0.2 / 1.0.3，GitHub 有四个）。不重拉就会给用户一个
   「来自旧仓库、点了会失败」的列表。
4. **「在跑」看得见**（用户第一句话）：两层反馈，可见性条件不同所以都要有 ——
   - 被点的图标按钮换成会转的 `loader`（`.obsync-spinning`）：**不受**「显示操作结果提示」设置影响；
   - 带圆环的进度提示，按文件更新文案（「Trefoil：正在获取 main.js…」）：受该设置控制
     （与设置页里那句说明一致）。
   逐文件进度本来就是现成的（`fetchFiles` 一直有 `onProgress`），这次才接到 UI：
   `InstallRequest.onProgress` → `fetchItem` → `fetchFiles`，`updateTheme` / `reinstall`
   各加一个可选参数；行内四条动作统一走 `runWithProgress`。

### 变异验证（四个方向）

| 变异 | 被哪条用例抓住 |
| --- | --- |
| `startSpinner` 不换图标（等于没有转圈） | 「点更新之后按钮变成会转的 loader」+「检查更新同样转圈」 |
| `install` 不再把 `onProgress` 传下去 | 新加的「取文件时逐个回报文件名」（见下） |
| `setMirror` 去掉 id 校验 | 「**id 不一致时拒绝采用**，记录一动不动」 |
| 换来源后不重拉版本列表 | 弹窗两条「…并按新来源重拉版本」 |

### 一次真实的测试盲区（值得记下来）

第一轮变异里，「不再传 `onProgress`」**没有被任何用例抓住** —— 因为那条 UI 用例的替身
服务自己调了 `request.onProgress`，验的是「UI 把回调交出去了」，**没验服务有没有一路
传到 `fetchFiles`**。补了一条走真实 `InstallerService`（只 mock HTTP）的用例：
`install({ onProgress })` 与 `reinstall(item, onProgress)` 都必须按
`manifest.json → main.js → styles.css` 回报。补完之后那个变异立刻被抓住。

教训与「装配层的接线」是同一类：**替身越靠近被测层，越容易漏掉中间那一跳**。

验证：`pnpm check` 6 项全通过；`pnpm test` **714 项全绿**（51 个文件）；`pnpm build` + 部署。

---

## 轮次：从 Gitee 镜像地址装 Trefoil 又失败 —— `String(undefined)` 是真值

用户贴来的日志（三条一样的形状）：

```
[OBSync] downloading asset manifest.json for sofqi/Trefoil failed,
         falling back to the source file at 1.0.3 …
installer error [assetDownloadFailed] {"repo":"sofqi/Trefoil","files":"main.js","of":"plugin"}
```

### 先量，再猜

同一台机器、同一时刻：

| 请求 | 结果 |
| --- | --- |
| `gitee.com/sofqi/Trefoil/releases/download/1.0.3/main.js`（匿名） | **200，963,711 字节，2.8 秒** |
| 同一个 URL 带一个**伪造**令牌 | 200（令牌不影响） |
| `gitee.com/api/v5/repos/sofqi/Trefoil/releases/1152200/attach_files/undefined/download` | **404** |

最后那条就是我猜的「插件实际会去请求的地址」——一次就命中。再看 Gitee 返回的资产原始 JSON：

```json
{ "browser_download_url": "https://gitee.com/sofqi/Trefoil/releases/download/1.0.3/main.js",
  "name": "main.js" }
```

**没有 `id`**（连 `size` 都没有）。而实现里写的是 `id: String(asset.id)` ——
缺字段时得到字符串 `"undefined"`，**它是真值**，于是：

```ts
if (token && release?.id && asset.id) {   // "undefined" 通过了这个判断
    url = `${API}/repos/${id}/releases/${release.id}/attach_files/${asset.id}/download`
    //                                    ↑ 字面量 undefined → 404
}
```

所以症状**只出现在配了 Gitee 令牌的人身上**（那条分支要 token 才进）——这也解释了
为什么我匿名测什么都好、用户却全 404。三个资产各 404 一次，回退源码后 `manifest.json`
在仓库里有、`main.js` 是构建产物没有 → `assetDownloadFailed: main.js`。与日志逐字对上。

### 修法（两处）

1. **缺字段就保持缺席**：新增 `optionalId()`，`mapRelease` 不再造 `"undefined"`；
   `GiteeAsset` 的 `id`/`size` 改成可选（照实测形状写类型）。
2. **附件端点失败时回落到公开下载地址**（带同一个令牌）：令牌过期、企业版差异、
   资产 id 变了，在 Gitee 上都表现为 404 —— 一条可选通道失败不该让整次安装失败。
   与 GitHub 的 raw → contents 回退同一个道理。

GitHub 侧不受影响：它的 `asset.id` 从来没被用来拼 URL（走 `apiUrl`），而且 GitHub
的 API 契约保证这些字段存在。

### 这一组用例之前**一条都没有**

`GiteeHost.downloadAsset` 在那个 bug 存在期间**零用例**。更值一提的是：**已经有的那条
映射用例把 fixture 按类型写的、不是按实测写的** —— 它给资产造了 `id: 991` 与
`size: 1234`，于是「字段缺席」这条路径从来没进过测试。

新增 6 条：映射（真实形状 → `id` 必须是 `undefined`，不能是 `"undefined"`）、
无 id 时不去碰附件端点（并断言 URL 里没有 `attach_files`/`undefined`）、
有 id 时优先附件端点、附件端点 404 → 回落、两条都失败才报错、无令牌不带 access_token。

### 变异验证（两个方向）

| 变异 | 被哪条用例抓住 |
| --- | --- |
| `optionalId(asset.id)` 退回 `String(asset.id)` | 「资产没有 id / size 时也是合法响应」→ `expected 'undefined' to be undefined` |
| 去掉附件端点的回落 | 「附件端点失败时回落到公开地址」→ 报出的正是用户日志里那句 `… was not found while downloading main.js: Not Found` |

### 一条通用教训（值得写进接手文档）

**测试 fixture 必须按实测响应写，不能按类型写。** 类型是我们自己写的（`id: number`），
平台可以随时不给这个字段；fixture 跟着类型走，就永远测不出「字段缺席」。
`String(x)` 这种「顺手转一下」的写法是这类 bug 的温床：它把缺席变成了一个真值字符串。

验证：`pnpm check` 6 项全通过；`pnpm test` **720 项全绿**（51 个文件）；`pnpm build` + 部署。

---

## 轮次：确认镜像的页面让人「去核对」，却没给能点开的地址

用户原话：

> 在让用户确认是否为镜像的页面中，应该提供疑似镜像的地址供用户打开跳转，方便用户核实

这一页自己写着「核对方式：打开镜像仓库，看它的作者、主页或 README 是否指向源仓库；
两边的最新版本号也不该差太多」—— 而两个地址都只是 `<li>` 里的**纯文本**。
用户要照做，就得手抄或复制粘贴到浏览器里。**在最需要「看一眼」的地方留了一道摩擦**，
而且这一页的全部意义就是让人去看那一眼（判据只有「两边 manifest 的 id 相同」，
证明不了是同一份代码、同一个作者）。

### 做法

- 新增 `ui/repoLink.ts`：`appendRepoLink(containerEl, ref, text, t, cls?)` →
  `<a href={repoWebUrl(ref)} target="_blank" rel="noopener" title="在浏览器中打开仓库">`。
  用真正的锚点而不是自己接 onClick —— Obsidian 会把外链交给系统浏览器，
  参考项目（obsidian-git `descEl.createEl("a", { text, href, attr: { target: "_blank" } })`、
  BRAT 同样）都是这么写的；顺带拿到**右键复制链接**与 Tab 聚焦。
- 确认页的清单改成「前缀文字 + 可点开的地址」（地址本身就是链接文本，不另挂按钮）。
- 版本管理弹窗里那条「发现疑似镜像：…」也一样 —— 那里也是用户做同一个判断的地方。
- 文案因此拆成前缀与地址两段：`mirrorSourcePrefix` / `mirrorCandidatePrefix` /
  `versionMirrorFoundPrefix`；被取代的 `mirrorConfirmSource`、`versionMirrorFound` 删掉。
  `mirrorConfirmCandidate`（拼好的整句）**保留** —— 「添加插件仓库」弹窗用它当设置行名，
  而且与确认页共用同一个模块级常量 `mirrorCandidatePrefix`，不会出现两份措辞。

### 两个替身的坑（都补了）

1. **`appendText` 替身里没有**：真实 Obsidian 有，而且「前缀 + 链接」这种写法必须用它，
   不补的话测试里一跑就 TypeError（真机反而正常）。按 `tests/setup.ts` 里那句
   「替身少了任何一个，对应那一页就只会在真机上炸」的规矩补上了。
2. **`createEl` 的顶层 `href` 选项替身不认**：第一版写成
   `createEl("a", { text, href, attr: { target } })`（照 obsidian-git 的写法），
   而替身只处理 `text`/`cls`/`attr` —— 于是「它到底指向哪里」这条最该钉住的性质
   **在单测里断言不到**（`attrs.href` 是 undefined）。改成把 `href` 也放进 `attr`：
   落进 DOM 的属性完全一样（`setAttribute`），而测试看得见。

### 变异验证

| 变异 | 被哪条用例抓住 |
| --- | --- |
| 把 `<a>` 换回纯文本（`createSpan`，即修复前的样子） | 确认页那条 + 版本弹窗那条同时失败（都找不到 `<a>`） |

验证：`pnpm check` 6 项全通过；`pnpm test` **721 项全绿**（51 个文件）；`pnpm build` + 部署。

---

## 轮次：删掉「重装」按钮（它与「版本管理」重合），圆箭头还给「检查更新」

用户原话：

> 版本管理和重装是不是功能有些重合，如果是的话，那么删除重装按钮，把重装按钮的图标
> 留给检查更新按钮使用。

### 先把「重合」查实（不要凭感觉）

| 动作 | 插件实际调用 | 主题实际调用 |
| --- | --- | --- |
| 重装 | `install({repo, version: tracked.requestedVersion, enableAfterInstall: true, …})` | `updateTheme(item)` |
| 版本管理（默认选中的**就是** `requestedVersion`） | 同一组参数调同一个 `install()` —— **逐字相同** | —（主题没有版本管理） |
| 更新到最新（主题那行的 ⬇） | — | `updateTheme(item)`，与重装**一字不差** |

所以不只是「有重合」，而是：**插件那边重装是本弹窗的一个特例，主题那边两件事是同一件事**。
删掉它**没有丢任何能力**：插件要修复安装就是「打开版本管理 → 直接点切换」，主题走
「更新到最新」。

### 做法

- 删按钮；删 `InstallerService.reinstall`（没有生产调用方了 —— 留着就是死代码，
  而这个项目的规矩是删掉或明确标注）；删 `reinstall` / `reinstalled` 两个 i18n 键
  （死键会被自查第 3 项拦住）。
- 图标：`refresh-cw` 还给「检查更新」。这个图标本来就是它的 —— 当年因为和重装的圆箭头
  撞车才换成放大镜（旧注释还在：「和『重装』的圆箭头几乎分不出来」）。重装没了，撞车消失。
- 行内动作的图标改成模块级 `ICON` 常量。

### 差点漏掉的一处（测试抓住的）

图标在**两处**出现：`setIcon()` 一次、`runWithProgress()` 一次（做完要把图标换回来）。
我只改了前者，转完圈又变回放大镜 —— `trackedItemsList.test.ts` 的「检查更新同样转圈」
当场报 `expected 'search' to be 'refresh-cw'`。

**这正是提 `ICON` 常量的理由**：同一条事实写在两处，就等着漂移。三个行内动作现在都取
同一个常量。

### 文档同步

README（中英）、HANDOVER（五点七节新增「为什么删掉重装」+ 图标那段的来龙去脉）、
RELEASE.md 的发版说明草稿都跟着改了 —— 用户可见的能力少了，说明书不能还写着。

验证：`pnpm check` 6 项全通过；`pnpm test` **720 项全绿**（51 个文件）；`pnpm build` + 部署。

---

## 轮次：把「部署到两个库」变成一条命令

用户问：「将插件部署在两个仓库的指令怎么写？」—— 之前每次都是 `pnpm build`（只部署测试库）
之后再手工 `Copy-Item` 到真实库，而那一步一直是**临时敲的**，没进过项目。

### 做法

`esbuild.config.mjs` 的部署插件原来只认一个目录（`OBSYNC_DEPLOY_DIR`，默认测试库）。
现在它认**多个**（`;` 分隔），并且：

- 每个目标**各自 try** —— 一个库写不进去（不存在、被占用）不连累另一个
  （原来是单目标一次 try，失败就整体跳过）；
- 分隔符固定用 `;`，**刻意不用 `path.delimiter`**：后者在 POSIX 上是 `:`，
  而 Windows 盘符 `F:/…` 本来就带冒号，用它会把一份配置切碎。

`package.json` 加了一条脚本：

```json
"build:both": "cross-env OBSYNC_DEPLOY_DIR=\"F:/…/Plugin-Test/…;D:/…/learning-records/…\" pnpm build"
```

`cross-env` 本来就装着（`test:live` 在用），引号里的 `;` 在 cmd.exe 下也能原样传过去 ——
实跑验证过：一条命令两个库都更新了，三处产物 hash 一致、`data.json` 没动。

文档同步：README 中英的开发一节（加了 `pnpm build:both`，写明「多个目录用 `;` 分隔」
以及**只复制三个产物、永远不碰 `data.json`**）、HANDOVER 第三节。

---

## 轮次：源码控制视图 —— 「能在侧边栏打开查看详情，就像 git 插件一样」

用户的原话：「对于仓库同步功能，我希望能在侧边栏打开查看详情，就像 git 插件一样」。

### 先查「为什么他找不到」，不要直接开始画界面

按这条抱怨去读界面，查出**三个**独立的真问题（一个都不是「不好看」）：

1. **侧栏唯一的图标打开的是安装器**。它的悬停文案是「OBSync：同步笔记仓库 / 安装插件」
   —— 一句话描述两件事，点开只有一件。想找同步面板的人点开看到一个装插件的弹窗，
   然后**在界面上没有任何办法**找到那个面板（它注册着、命令也有，但没人知道它叫什么）。
   而它的命令名取自 `t.sync.viewTitle`，那个键的值是 **「OBSync」** ——
   命令面板里搜「同步」也搜不到。
2. **状态栏条目不可点**。它是屏幕上唯一常驻的同步入口（`OBSync: main ~3`），
   却没有任何交互。
3. **面板内容单薄**：只有一个标题、四个按钮、一串文件名。没有 ahead/behind、
   没有远端地址、看不出哪些文件已暂存 —— 而 README 写着「逐个文件暂存 / 取消暂存」，
   代码里**根本没有暂存这个动作**（`SyncService` 没有 `stageFiles`）。
   不是 git 仓库时只有一句提示，**没有出路**（不能从面板里初始化）。
   另外视图里的分支切换是**直接调 `git.checkout`**，绕过了 `SyncService` 的串行队列。

### 做法

- `SyncService` 加三个**走队列**的动作：`stageFiles` / `unstageFiles` / `checkoutBranch`
  （空数组早退，不额外弹提示 —— 结果在界面上看得见）。
- `SyncService.onStatusChange(listener)`：推送式状态订阅（`refresh()` 里统一 publish，
  订阅者抛错被包住）。视图是常驻的，状态会在它背后变（自动提交、库外改动、命令面板）。
  参考项目 obsidian-git 靠固定间隔轮询，这里有真实变化点所以改成推送。
- `SourceControlView` 重写：头部（刷新 + 立即同步）、动作行、状态摘要（分支下拉 / 远端地址
  **脱敏**回显 / 领先落后）、冲突区（文件列表 + 放弃合并）、**按「已暂存 / 更改」分组**的
  文件列表（逐个暂存 / 整组暂存 / 点文件名打开笔记 / 在远端打开）、最近 10 条提交
  （点 hash 在远端查看 —— `commitOnRemoteUrl` 此前是**死代码**，只有测试引用它，
  正好接上）。不是仓库时给「初始化仓库」按钮。
- 入口三处都补上：侧栏**两个**图标（`git-fork` 开面板、`download` 开安装器）、
  状态栏条目可点（`SyncDeps.openSourceControlView`）、命令名改成新的 `cmdOpenView`。
- `viewTitle` 与 `cmdOpenView` **必须是两条键**：命令面板要能搜到 → 必须有 `OBSync：`
  前缀（`pluginBoot.test.ts` 有一条用例扫所有命令名），而面板标题不该带前缀。
- 视图取文案改成 `getT()`（原来传的是 `this.t`，快照 —— 常驻面板切语言后不会变）。

### 两个渲染守卫（各挡一个真实故障）

- `rendering`：渲染里会 `refresh()` → 通知订阅者 → 又渲染 → **无限递归**。
  实测去掉它，测试直接 `RangeError: Maximum call stack size exceeded`；
  因为跑在 promise 链里，界面上只表现为**面板一片空白**。
- `acting`：自己发起的动作期间不让订阅重绘（动作结束后的那次 `render()` 就够了），
  否则每次动作白跑一遍 `git status`。

### 一处刻意的不对称

**冲突行不给暂存开关。** `git add` 会让 git 认为冲突已解决，而面板看不到文件内容 ——
用户可能没改就把带 `<<<<<<<` 的文件暂存、然后提交上去（`doCommitAll` 只拦
「仍有冲突」，暂存之后它就拦不住了）。冲突区只负责「哪些文件要处理 + 给出路」。

### 替身补了三处（都是"真机才有、替身没有"的东西）

- `ItemView.contentEl` / `ItemView.app` —— 缺了它，「渲染一次看看画出了什么」
  这类测试连构造都过不去（所以这个面板此前**一个渲染测试都没有**）。
- `ButtonComponent.setWarning()`（放弃合并要警示色）。
- `Plugin.addRibbonIcon` 改成记录 `{icon, title, onClick}` —— 只数个数漏掉了
  「唯一那个图标打开的是安装器」这个真问题。`fakeApp` 的工作区也补了右侧叶子的替身
  （`setViewState` 记录下来），这样「点图标真的会请求打开 `obsync-sync-view`」才验得了。

### 变异验证（四条）

| 变异 | 结果 |
| --- | --- |
| 去掉 `rendering` 重入保护 | `RangeError: Maximum call stack size exceeded` |
| `stageFiles` 不走 `enqueue` | 「慢提交没结束时暂存不会插队」失败 |
| `changeRows` 的 `staged` 恒为 false | 3 条失败（分组 / 行按钮 / 整组动作） |
| 同步图标改回打开安装器 | 「侧栏的同步图标真的会请求打开源码控制视图」失败 |

「队列真的串行吗」不是看结果就行的：用例让 `FakeGit.status()` 卡在一个 promise 上
（`waitBeforeStatus`），此时提交还占着队列，断言暂存**还没发生**。

### 一处发现

`Setting.setClass` 最终落到 `classList.add()`，**带空格的字符串在真实 DOM 里会抛
InvalidCharacterError** —— 所以写成两次 `setClass`，不要写 `setClass("a b")`。
（`createEl({ cls: "a b" })` 是另一回事，那条路径 Obsidian 自己会拆。）

数量：新增 37 条用例（720 → 757），`pnpm check` 六项全过（CSS 类 49 用 / 49 定义 ——
旧的 `.obsync-changes` 规则随之删掉，没有留下僵尸类），`pnpm build:both` 已部署到两个库
（`data.json` 未动），HANDOVER 新增五点八节。

---

## 轮次：「尝试推送后一直看到正在推送」

用户报的是这一句。里面藏着**两个互不相干**的故障，都查实了。

### 先量现场，别猜

- 他的库（`learning-records`）远端是 Gitee，`git ls-remote` 匿名 **1.7 秒**就通 ——
  不是网络不通；
- `rev-list --left-right --count master...origin/master` = **`0 0`** ——
  **本地和远端一致**，所以他点的那次推送**根本没有东西可推**；
- 没有残留的 git 进程；
- `credential.helper` 指向 **GCM**（PortableGit 带的）。

`0 0` 这条把第一个成因钉死了：推送早就结束了，界面却还在说它正在进行。

### 成因一：状态栏的活动态没有终点（真 bug）

`activity` 是 `StatusBar` 的实例状态，而 `render()` 在活动态下**只显示活动文案
并 return** —— 没人改回 `idle` 就永远是「正在推送…」，连分支 / ahead / behind /
脏文件数都不再显示，直到重载插件。`git log -S 'setActivity("idle")'` 查过：
**这个调用在 src 里从来没有出现过**（只有测试手工调）。

为什么没被发现：`statusBar.test.ts` 那条「动作结束后能恢复」是**手工调**
`setActivity("idle")` 验的 —— 验的是「这个 API 能恢复」，不是「服务真的会调它」。
**测试写在了 API 上，而不是写在现场上**（这个项目反复踩的同一个坑）。

修法：`SyncService.withActivity()`，在 `finally` 里恢复 idle + 刷新状态；
4 条新用例（提交/拉取/推送/同步 + **出错路径**）。变异：删掉那行 → 3 条失败。

### 成因二：git 可能真的在等人回答（加护栏）

git 拿不到凭据会**提问**（终端提问 / GCM 弹窗），而 Obsidian 里没人能回答：
提问读的 stdin 是一根没人写的管子，命令挂着，`isBusy` 永远 true，
后面所有同步都排队等它，自动定时器也一直跳过。用户只能重载插件。

- `GIT_TERMINAL_PROMPT=0` + `GCM_INTERACTIVE=never`：**禁的是「问人」，不是「用凭据」**
  （已存的凭据照常用 → 依赖系统凭据助手的那类用户不受影响）；
- simple-git 的 `timeout.block = 120s`（**无输出**超时，传输有输出就重置）
  → 卡死变成「超时 + 已中止 + 去查网络」（新增 `GitTimeoutError`）；
- push / fetch 都补 `--progress`：git 在 stderr 不是终端时**默认不打进度**，
  不带它的话一次慢但正常的传输会被无输出超时误杀；
- 两条 spawn 路径统一到 `createGitInstance()`（原来 `git()` 与 `rawGetRemoteUrl()`
  各自建实例，逐处补设置必然漏一处）。

### 成因三（顺带）：点了推送却什么都没发生

本地没有新提交时 `doPush()` 直接返回，界面**一点变化都没有**（状态栏还卡着），
用户没法区分「没东西可推」和「卡住」。现在用户主动入口会说一句
「没有需要推送的内容」；自动定时器不说（每 N 分钟弹一次是噪音）。

### 验证的一个边界（值得记）

环境变量到底有没有传到 git 子进程，光验「我们调了 `.env()`」不够。
想用 `-c alias.x=!echo $GIT_TERMINAL_PROMPT` —— **simple-git 默认禁止配 alias**
（`allowUnsafeAlias`）。改用 **git 钩子**当观察点（钩子是 git 用 `sh` 起的子进程，
拿到的正是 git 进程的环境），真 git 跑一遍断言它打出 `0/never`；
去掉 `.env()` 那两行它打出 `/`（实测）。

数量：新增 18 条用例（757 → 775），`pnpm check` 六项全过，typecheck 干净。

---

## 轮次：「推送按钮是单纯的推送还是提交全部加推送？」

用户的追问。答案是**单纯的推送**（`doPush()` 里只有 `git push -u origin <branch>`，
不含任何提交），但这个问题本身就是结论：**他是问了才知道的**，说明界面没写清楚。

### 三处补上「写清楚」

1. 视图三个动作按钮的悬停提示：`actSyncHint` / `actCommitHint` / `actPushHint`。
   「推送」那条必须点明「只推送**已提交**的内容，不会自动提交」。
2. 用户主动推送时按状态各说一句：
   - 无提交 + 工作区干净 → 「没有需要推送的内容」；
   - 无提交 **但**有 N 个未提交改动 → 「推送只发送**已提交**的内容，而你有 N 个更改
     还没提交」。**这一条是这次的关键**：原来它说成「没有需要推送的内容」，
     听起来像「一切正常」，而真实情况是「你的改动一个都没上去」；
   - 推送成功但仍有未提交改动 → 「已推送到远端，另有 N 个尚未提交」；
   - 推送成功且干净 → 「已推送到远端」（原来成功后完全没反馈）。
3. README 中英写清「提交」与「推送」是两个动作。

### 顺手消掉一个重复规则

提示里的 N 必须与面板里看到的列表一致（否则用户拿着两个数字对不上，会以为插件乱数）。
原来 `changeRows` 住在视图里，服务层要用就得复制一份规则 —— 所以把它搬到
`src/features/sync/changeRows.ts`，视图与提示共用一份，用例也跟着搬
（`tests/features/changeRows.test.ts`）。

变异：去掉 `setTooltip(actCommitHint)` → 悬停提示用例失败；去掉
`pushNeedsCommit` 分支 → 2 条失败。

---

## 轮次：提交信息要不要填？加一个「提交并推送」按钮

两个问题一起答。

### 「提交是不是必须填写备注信息？」

**不用填，全程没有输入框。** 提交信息由 `settings.sync.commitMessage` 模板自动展开
（默认 `vault backup: {{date}}`，支持 `{{date}}` / `{{hostname}}` / `{{numFiles}}` /
`{{files}}`）。要改措辞就去设置页改模板。顺带解释了日志里那些
`vault backup: …` 的来历 —— 那是模板默认值。

### 「加一个提交并推送的按钮」

`commitAndPush()` = `sync()` **去掉拉取**（`doCommitAll()` → `doPush()`）。

为什么值得单列，而不是「想推就用立即同步」：**拉取会动工作区**（merge 可能产生
合并提交、rebase 改写本地提交、reset 丢弃本地提交），而有些人要的只是
「把我的改动变成提交再送上去，别来合并我的文件」。代价写在按钮提示里：
远端真有新提交时推送被拒 —— **故意**的，报错比悄悄合并安全。

两个实现细节（都有用例钉着）：

- **「没有更改可提交」不能提前返回**：工作区干净不代表没有要推的东西
  （上次的提交可能还没推上去），所以不能像 `commitAll` 那样在 nothing-to-commit 收工；
- **活动态按阶段走**（committing → pushing → idle），不能整条链路显示「正在提交」。

视图布局也跟着调整：**一步到位的两个**（立即同步、提交并推送）放一行，
**三个单一动作**（提交全部、拉取、推送）放另一行 —— 四个按钮挤一行在侧边栏里太窄。

变异：把 `commitAndPush` 改成先 `doPull()` → 「完全不碰远端拉取」那条失败。
命令面板加了同名命令。

---

## 轮次：「立即同步为什么是提交 → 拉取 → 推送？拉取是为了什么？」

用户的设计追问。答案分三层，都补进了 README（中英）与 HANDOVER：

1. **拉取是为了让推送能被接受**：git 的 `push` 只能快进 —— 远端有你没有的提交时
   推送被**直接拒绝**（接受等于丢掉那些提交）。而这个插件的用途就是多设备同步，
   所以「提交 → 推送」在两台设备上是**稳定失败**，不是偶发失败。
2. **拉取排在提交之后**（顺序里真正有讲究的一处）：先提交 → 改动进了可恢复的提交，
   之后整合远端动的是提交层面的事，出问题还能「放弃本次合并」回到拉取前；
   先拉取的话，未提交改动会和冲突标记混在一起，谁也分不清，也没有一键回退的路。
3. **代价**：拉取会动工作区，策略可配（默认 merge；`reset` 会丢弃本地提交）。
   这正是「提交并推送」存在的意义 —— 不想让拉取碰工作区时跳过它，
   代价只是远端有新提交时推送被拒。

顺带记住：拉取之后可能**再提交一次**（整合产生的合并提交要一起推上去），
自动定时器走完整链路（无人值守时更需要先拉取，否则每 N 分钟一次「推送被拒」）。

---

## 轮次：「提交并推送」加了又删；四个动作同一行；`提交全部` → `提交`

用户听完「为什么要有拉取」之后自己下了结论：**立即同步已经是万全之策**，
「提交并推送」是多余的。于是：

- 删掉按钮，**同时删掉命令与服务方法**（`commitAndPush`）—— 留着没人调的方法
  就是死代码，这个项目第七节专门有死代码清点；
- 四个动作**同一行**：`立即同步` / `提交` / `拉取` / `推送`（他明确否掉了分两行）；
- `提交全部` 的按钮文案改成 `提交`（行为没变，仍是暂存全部 + 提交；
  命令面板里那条仍叫「提交全部更改」—— 那里空间够，写清楚更好）。

两个技术点：

- 窄侧边栏里四个按钮放不下，而 Obsidian 的 `.setting-item-control` 默认**不换行**
  → 按钮会被压扁或溢出。加了 `.obsync-actions` 允许换行：**换行总比溢出把按钮挤没了好**；
- 布局本身写了用例（断言那一行正好是四个按钮、顺序固定）—— 变异：把行拆成两行，
  用例立刻失败。

删掉的东西在 HANDOVER 里记成「加了又删」并写明**别再加回来**的理由：
需要「不拉取」的人用 `提交` + `推送` 两步即可。

---

## 轮次：展示仓库体积 + 「与远端一致」的醒目反馈

用户：「能否展示提交仓库的大小，并且当提交结束与远端一致时，给出醒目的反馈」。
体积那半有两种理解，先问了一句（多选）—— 他选了**两者都要**。

### 两个体积回答两个不同的问题

- **仓库大小**：`git count-objects -v`（松散 + pack，KiB → 字节）+ 对象数。
  **不加 `-H`**：那会输出 `12.34 MiB` 这种字符串，还得反过来解析它的单位选择规则；
  用默认 KiB 自己换算更可靠。解析是纯函数，fixture **逐字照抄真实输出**。
- **待提交改动**：把 `changeRows(status)` 里各文件大小加起来（`vault.adapter.stat`）。
  git 不直接给字节数，所以这是**近似值**（实际传输量还看压缩率；删除的文件没有体积）。
  取不到大小按 0 计 —— 这只是个参考数字，不该因为它失败而让面板画不出来。

**读不到时说「读不到」，绝不显示 `0 B`**：0 B 会被当成「空仓库」，那是个错误结论。

### 「与远端一致」的判据只有一处

三个消费方（同步结束的提示、状态栏 `✓`、面板绿色高亮）必须同一个答案 ——
所以判据收在 `syncState.ts` 的 `isFullyInSync`。判据是**全部满足**：
有 upstream、不领先、不落后、无冲突、无未提交改动。

顺手修掉一句**假话**：原来「推送」在 `ahead === 0` 时说「没有需要推送的内容，
**本地与远端一致**」—— 而 `ahead === 0` 完全可能还**落后**远端。现在那句只说
「本地没有新提交」，真正的「一致」由 `syncedInSync` 说。

### 反馈的三种强度

1. **醒目提示**（`Notifier.synced`）：绿色对勾元素 + 停留 8 秒（普通提示 5 秒）+
   文案带 ✓ 与仓库体积。普通 success 一闪而过，而这一条是**结论**；
2. **状态栏 `✓`**：一切正常时才有 —— 否则「同步完了吗」只能靠「没有任何标记」回答，
   而那和「还没看过」长得一样；
3. **面板那一行转绿加粗**。

后两种**不受「显示操作结果提示」设置影响** —— 关掉提示的人也能看到状态。

`after` 回调只在**成功**时跑（失败后说「与远端一致」只会让人困惑），拿到的状态是
动作**之后**刚刷出来的那份 —— 所以判断的是结果，不是动作前的状态。

### 替身的两处保真度问题（都是这次才暴露的）

- `FakeGit.status()` 原来恒返回 `behind: null`（= 没有 upstream），于是「一致」永远
  不成立；改成有远端时 `behind` 也是数字。
- `FakeGit.commit()` / `push()` 原来不改 `ahead` —— 提交后不领先、推送后不归零，
  「提交后是否与远端一致」在测试里永远算成一致，**而那正好是反的**。
- `fakeApp` 的 adapter 补了 `stat`（返回真实 UTF-8 字节数）：空实现会让体积永远
  显示 `0 B` 而测试全绿 —— 这类「数字是假的」比报错更难发现。

验证：820 条用例（+36）。变异：去掉 `isFullyInSync` 里「无未提交改动 / 无冲突」
两条 → 6 条失败。

---

## 轮次：状态栏「占满整屏宽」做成设置页「通用」里的开关

用户：「将状态栏的全宽添加一个设置项开关到通用标签页里」。

### 做法：给 body 加类，而不是运行时改内联样式

那条规则用到了 `:has()`（`.status-bar:has(.obsync-status-bar-item)`），只能写在 CSS 里。
所以开关的做法是 `main.ts` 的 `applyStatusBarWidth()` 给 `body` 加/摘
`obsync-status-bar-full-width`，CSS 那条规则挂在这个类下面 ——
**没有第二条规则要同步维护**。

不选「运行时改 Obsidian 核心元素的 `style`」的理由：那会与主题/其他插件互相覆盖，
而且插件卸载后留下的内联样式更难清干净。

### 三件必须钉住的事

1. **默认开着**（与既有行为一致）—— 加开关不该悄悄改掉任何人的界面。老的
   `data.json` 里没有这个字段，`normalizeSettings` 补成 `true`；有用例钉着
   「补成 false 就等于改了所有人的界面」这条判断。
2. **拨开关立刻生效**（`commit()` → `applyDerivedSettings()` → 加/摘类），
   不用重载插件、也不用重开设置页。
3. **插件卸载时也要摘掉**（`onunload` 里 `applyStatusBarWidth(false)`）——
   不摘的话禁用插件后状态栏会莫名保持全宽，而谁也看不出是谁干的。

关掉之后条目仍然在那一簇的**最前面**（`order: -1` 还在），只是不再贴屏幕最左 ——
功能不丢，只是不再占满一整条。设置页的描述文案特意写清「会失去什么、不失去什么」：
只说「恢复原样」会让人以为同步条目没了。

### 设置页「通用」这页此前一个用例都没有

补了三条：三行都在（提示 / 调试日志 / 状态栏全宽）、默认值是 true 且描述文案一致、
**拨动后设置真的写进去并且 commit 被调用**。最后一条最容易漏 ——
设置页里 `onChange` 忘了赋值或忘了 `commit()` 都是常见错，而那表现为「拨了没用」，
与当年那个「启用笔记同步」的死开关是同一类问题。

（写这条用例时暴露了替身缺 `saveSettings` / `applyDerivedSettings`，
异步 onChange 抛错成了 unhandled rejection —— vitest 会警告「可能导致假绿」。
顺手把这两个补成**会记数**的替身，于是「有没有落盘 / 有没有重算派生状态」也验得了。）

验证：826 条用例（+6）、check 六项全过（CSS 类 54 用 / 54 定义）。
变异：`applyStatusBarWidth(this.settings.statusBarFullWidth)` 改成恒 `true` →
boot 用例失败；toggle 里去掉 `await this.commit()` → 设置页用例失败。

---

## 轮次：视图改名「仓库同步」+ 顶部三行并一行

用户原话：**「把侧边栏的源码控制视图改名成仓库同步，并把源码控制标题删除。标题右边的
刷新按钮和立即同步同一行的四个按钮，以及下一行的分支下拉表合并为一行展示。」**

### 改了什么

- `viewTitle`「源码控制」→「仓库同步」。**连带改了三条**：`cmdOpenView`、
  `statusBarHint`、`ribbonSync` —— 它们说的都是同一个面板，只改一条会留下
  「打开源码控制面板」这种对不上号的入口。源码/测试/CSS 注释里的「源码控制视图」
  也一起改成「仓库同步视图」（`CHANGELOG` 与 `PLAN.md` 是历史文档，没动）。
- **面板内那个 `.setHeading()` 标题删了** —— 标签页上已经写着视图名，侧边栏一行
  标题就是一行浪费。于是 `viewTitle` 只剩 `getDisplayText()` 一个用处。
- 刷新 + 四个动作 + 分支下拉并进**同一个 `Setting`**（`renderHeader` → `renderToolbar`）。

### 两个不是「顺手改」的连带后果

1. **渲染顺序变了**：原来先画头部再 `refresh()`，现在**先 `refresh()` 再画工具条**。
   分支下拉属于状态 —— 不是仓库时那一格必须空着，而不是显示一个编出来的分支名。
2. **分支那一行的标签与 `desc` 没了**（游离 HEAD 原来写在 `desc` 里）。一行里放不下
   可见标签，于是：标签挪到下拉框的 `aria-label`；**游离 HEAD 改成「下拉框里唯一
   那个选项写着「游离 HEAD」并置灰」** —— 这条信息必须留个落点。列不出分支
   （`git branch` 出错 / 仓库还没有分支）同样置灰：能点但里面只有它自己，是个假象。

### 踩到的坑 / 记住的

- **`tests/stubs/obsidian.ts` 的 `DropdownComponent` 缺 `selectEl` 与 `setDisabled`**
  （真实组件两者都有）。少了它们，「标签挂在 aria-label 上」和「置灰」这两条路径
  在测试里根本跑不到 —— 与当年「替身少了 `noticeEl`，SpinnerNotice 一跑就 TypeError」
  是同一类盲区。补替身时要连**注释**一起写清「真实组件为什么有它」。
- 替身里 `addExtraButton` 就是 `addButton`（都进 `buttons` 数组，按调用顺序），
  所以工具条的 `buttons[0]` 是刷新图标按钮、`text === ""`。旧用例里那句
  `buttons[0].text !== ""` 的过滤条件正是被这条规则坑掉的。
- `.obsync-actions .setting-item-info { display: none }`：这条工具条没有名称与描述，
  而 `.setting-item-info` 是 `flex: 1`，留个空盒子只会白占宽度把控件提前挤到换行。

验证：830 条用例全绿、check 六项全过。变异：把分支下拉挪出工具条（另起一个
`Setting`）→ **4 条**用例失败（「工具条一行」+ 三条分支下拉的用例）。

---

## 轮次：工具条顺序 + 体积两栏（同一天第三次）

用户原话：**「立即同步按钮放到右侧，刷新按钮放到最右侧。仓库大小和待提交改动这两块
分两栏横向排布成一行。」**

### 改了什么

- 工具条调用顺序（= 界面从左到右）改成
  `提交 → 拉取 → 推送 → 分支下拉 → 立即同步 → 刷新`。
- **「在右侧」不等于「排在最后」**：给 `立即同步` 的按钮挂 `obsync-action-sync`，
  CSS 里 `margin-left: auto` —— 面板够宽时把富余空隙全吃掉顶到最右，顺带把
  「分步动作」和「一键走完」在视觉上分开；窄面板没富余空间，等于没写。
  取按钮元素是 **`ButtonComponent.buttonEl`**（`extraSettingsEl` 是
  `ExtraButtonComponent` 的，别混）。
- 体积两块改成挂在同一个 `.obsync-metrics`（横向 flex，两栏 `flex: 1 1 0` +
  `min-width: 0`）。两个容易漏的细节：`align-items: flex-start`（两栏标签对齐）、
  去掉 `.setting-item` 的上边框（Obsidian 只给非首项加，并排时只有右栏有线，像画歪）。

### 记住的

- **`addExtraButton` 与 `addButton` 落在同一个 `controlEl`、按调用顺序 append** ——
  所以「谁在谁左边」就是调用顺序。替身原来把 `buttons` / `dropdowns` 分开存，
  验不了「分支下拉夹在推送和立即同步之间」这种排布；补了 `Setting.controls`
  （控件调用顺序）之后才验得了。**排布本身就是需求时，替身得能还原完整的一条。**
- 给替身 `ButtonComponent` 补了 `buttonEl`（真实 `ButtonComponent` 有 `buttonEl`、
  `ExtraButtonComponent` 才有 `extraSettingsEl`；替身把两个组件合成一个，
  所以两个属性都留着，注释里写清了谁属于谁）。
- 部署产物核对：`main.js` 的 mtime = 构建时刻（可靠），但 **`styles.css` 是原样拷贝
  并保留源文件 mtime** —— 看它的时间戳会误判成「没部署」。用 `diff` 比内容才可靠。

验证：831 条用例全绿、check 六项全过（CSS 类 56 用 / 56 定义）。变异（各自单跑只红一条）：
「立即同步」挪回最前 → 顺序用例失败；「待提交改动」塞回 `contentEl` → 两栏用例失败；
去掉 `obsync-action-sync` → 专用类用例失败。

### 事后自检抓到的两处

1. **`Setting` 的默认排版是「名称大而深、描述小而灰」**（名称 `--font-ui-small` +
   `--text-normal`，描述 `--font-ui-smaller` + `--text-muted`）。用在「标签 + 数值」
   这种两栏上是**反的** —— 照默认渲染，一眼看到的是「仓库大小」四个字而不是
   「12 MB」。要反过来写：名称压到 `--font-ui-smaller` + `--text-muted`，
   描述提到 `--text-normal`。**用 `Setting` 做「指标卡」时必踩，记着点。**
2. `docs/HANDOVER.md` 第 991 行还写着「两个体积…所以面板里是**两行**，不是一行」——
   布局改了但文档没跟上。**改布局时要 grep 一遍「两行 / 一行 / 分栏」这类描述。**

---

## 轮次：问答「Obsidian 每 2s 保存一次，会不会和自动同步打架」

只查证 + 分析，没改代码。结论：**两者不是一条链路，不会互相干扰**；但顺手查出
两个**潜在**问题（都还没修，也没跟用户确认要不要修）。

### 查证到的（都在代码里，可复查）

- 插件**完全不监听库文件事件** —— `src/main.ts` 里唯一的 `registerEvent` 是
  `workspace.on("file-menu")`（右键菜单）。所以「2 秒一存」对同步模块**不可见**：
  既没有事件风暴，也不存在「保存一次触发一次提交」。
- 自动同步是**分钟级定时器**：`autoCommitMinutes` / `autoPushMinutes` /
  `autoPullMinutes`，取值 0–1440（`settings.ts` 的 `clamp`），默认全 0（关），
  设置页是 `<input type="number" min="0">`。**最快 1 分钟**，比 2 秒慢 30 倍。
- `automatics.fire()` **只判 `service.isBusy`**，没有「库安静了没」的判断 ——
  也就是说它**可能在你正打字时开跑**。`isBusy` 为真则整轮跳过（不排队）。
- 所有 git 动作走同一条串行队列（`enqueue` + `pending` 计数器），所以插件自己
  不会有两个 git 进程抢 `index.lock`。
- 自动的 "commit" 那档实际是**完整同步**：commit → pull →（若拉到了再 commit 一次）→ push。
- `stage([])` 的语义是 `git add -A`（整个库）。

### 两个潜在问题（未修，等用户决定）

1. **拉取策略选 `reset` 时，自动同步每轮都会丢掉刚做的提交。**
   链路是先 `doCommitAll()` 再 `doPull()`，而 reset 策略走
   `resetToRemote()` → `git reset --hard upstream` —— 把刚提交的那个 commit
   变成不可达（只能靠 reflog 找）。默认策略是 `merge`，所以只在用户主动改成
   reset 且开着自动同步时发生。**不是 2s 保存引起的，但值得提醒。**
2. **`.gitignore` 模板没挡 `.obsidian/plugins/*/data.json`。**
   `zh-cn.ts` 的 `gitignoreTemplate` 只挡了 `workspace.json` /
   `workspace-mobile.json` / `.trash/` / 系统垃圾文件。别的插件写自己的
   `data.json`（以及 `.obsidian/` 下其它会被频繁写的东西）都会进 diff、
   被自动提交。测试库那份旧 .gitignore 里反而有
   `.obsidian/plugins/gitee-sync-plus/data.json` 这一行 —— 说明**以前有、后来
   改名时漏了**。

### 关于「2 秒」这个说法本身

社区里被反复引用的描述（Autosave Control 插件作者）：Obsidian 默认在**开始输入
后约 2 秒**首次保存，之后**每 2 秒**一次，直到没有新改动为止；切换/关闭/退出时
会冲刷。官方**不提供**设置项调它（想改只能靠第三方插件）。

---

## 轮次：注意事项写在设置页标题下 + reset 与自动同步互斥（同一天第五次）

用户原话：**「那其实直接把注意事项写在仓库同步的标题下就好了是吧？」**

### 先纠一个预判偏差

「仓库同步的标题」只可能指**设置页**那个 heading —— 侧边栏面板里的标题**是上一轮
他自己要求删掉的**（`renderHeader` → `renderToolbar`）。判断依据就是那条已删的标题，
不是猜的。

更值得说的是**内容**：他最初担心的那条（2 秒保存冲突）恰恰**不用写** —— 上一轮已经
查实不成立（插件不监听文件事件、自动同步是分钟级、`isBusy` 时整轮跳过）。照原意写一条
「2 秒保存可能冲突」等于给他一条**错误信息**，让他去防一件不会发生的事。
所以这里没照字面做，而是换成了真正查出来的那两条。

### 三条事项的定性（性质不同，处理方式就不同）

| 事项 | 定性 | 处理 |
| --- | --- | --- |
| `.gitignore` 模板漏 `data.json` | **真回退**，能修 | 直接补 |
| `reset` 策略 + 自动同步 | 语义如此，但**静默周期发生** | 写说明 **+ 硬防护** |
| 多设备同时编辑同一文件 | **推断，未实测** | 只写说明，措辞保守 |

用户的选择：写前两条、`.gitignore` 直接补、reset 那条**要硬防护**（三问全选了最硬的）。

### 硬防护必须做在逻辑层（这是同一个坑的第二次）

只把设置页的开关灰掉是**假防护**：库里**已经**存着 `enabled: true` + `reset` 的用户
根本不会去动设置页，定时器照跑，而界面看起来一切正常。

`Automatics.enabled` 的注释里早就写着这个教训（「**这个字段必须在这里也有一份，
而不是只在设置页判一下**」），所以照抄那个模式：`AutomaticsSettings` 加
`syncStrategy` 字段，`start()` 里 `reset` 时一个定时器都不起。

两个刻意的选择：

- **灰开关时不动它的值**（`setDisabled` 而非 `setValue(false)`）：用户改回「合并」后
  自动同步**自己就恢复**。这是文案里写下的承诺，写成 `setValue(false)` 会让它变成谎话。
  有专门一条用例钉住。
- **策略下拉的 `onChange` 改 `commit(true)`**（重绘）：策略决定总开关是否可用，
  不重绘的话切到「重置」后开关看起来还能拨 —— 而行为已经停了，比不灰掉更困惑。

### 注意事项的位置与样式

放在标题**正下方**（`renderSync()` 里、`isSyncAvailable` 检查之后）。不塞进各设置项的
描述里：两条都是**组合条件**才踩得到的坑，写进单项描述没人读得到 —— 用户是在配好
之后才出问题，那时早就不翻设置了。

`.obsync-settings .obsync-sync-notes`（左侧竖线 + 正常文字色）。**正文刻意不用
`--text-muted`**：灰字正是「扫过去看不见」的原因，而这两段说的是「这样配会丢东西」；
也不用 `mod-warning` 的橙色 —— 那是「现在就出错了」，而这里是「别这么配」。

### `.gitignore` 那条是回退

测试库那份旧 `.gitignore` 里明确有 `.obsidian/plugins/gitee-sync-plus/data.json`
（和 `workspace.json` 写在同一段注释下），而 `gitignoreTemplate` 只有前两条 ——
改插件 id 时漏搬了。现在补 `.obsidian/plugins/obsync/data.json`。
**刻意不写 `*/data.json`**：那会波及用户库里其他插件的设置，不该替他决定。

### 验证

`automatics.test.ts` +2、`settingsTabRender.test.ts` +5（新增「设置页 · 仓库同步页」
一组 —— 这一页此前**一个用例都没有**）。838 条全绿（55 文件）、check 六项全过
（CSS 类 58 用 / 58 定义）、`pnpm build` + 部署。

变异验证（各自单跑都只红对应用例）：

| 变异 | 结果 |
| --- | --- |
| 去掉 `start()` 里的 reset 判断 | 2 条失败（挂起 + 改回后恢复） |
| `setDisabled(suspended)` → `setDisabled(false)` | 「总开关被禁用」失败 |
| 在注意事项前插一个 `Setting`（破坏「紧邻标题」） | 「紧跟在标题下面」失败，`expected '' to be 'obsync-sync-notes'` |

### 记一条方法上的事

用户提的方案（「写在标题下」）**位置对、内容错**。直接照做会写进一条错误信息 ——
而这类偏差只在「先自己核一遍事实」之后才看得见。上一轮已经把事实查清楚了，
这一轮的价值全在**没有照字面执行**上。

---

## 轮次：查出一个「HEAD 是红的」漏提交，并把它变成可检测的

用户问「为什么你不能帮我做？这种情况以后还会出现吗？」—— 第二问促成了这一轮。

### 先查清一个矛盾

`git diff` 显示 `src/settingsTab.ts` 里那个 `statusBarFullWidth` 设置项是**未提交**的，
而 `d939df2` 的标题正是「状态栏全宽做成设置页开关」。查：

```
git show --stat d939df2                                   → 11 个文件，含 settingsTabRender.test.ts (+79)
git show HEAD:src/settingsTab.ts | grep statusBarFullWidth → 空
```

**那次提交把测试与 i18n 键都提交了，唯独漏了 `src/settingsTab.ts`。**

实跑确认（`git stash push -- src/settingsTab.ts` 后单跑那个测试文件）：3 条「通用页」
用例红，其中一条报得极清楚 ——
`expected [ '界面语言', '通用', '显示操作结果提示', '输出调试日志' ] to include '状态栏占满整屏宽'`。
再 stash 全部跑全量：`Test Files 1 failed | 54 passed (55)`，只坏这一个文件。

### 为什么它能潜伏

**`pnpm test` 读的是工作区文件**，而工作区是完整的 —— 所以本地永远绿。
只有别人克隆、或 CI，才会撞上红的 HEAD。**「本地全绿」和「HEAD 全绿」不是一回事。**

### 做法：`pnpm verify:head`（`scripts/verify-head.mjs`）

把工作区（**含未跟踪文件**）stash → 跑测试 → **无论成败都 pop**。

- `--include-untracked` 不是可选的：新加的测试文件若还没 `git add`，不 stash 掉就会
  混进「HEAD 的测试」里，那验的就不是 HEAD 了，等于白跑；
- pop 在 `finally` 里，且**不吞掉测试的退出码**（两个职责：报 HEAD 的真实状态 +
  保证工作区原样还回来，缺一个都没人敢用）；
- 出问题时把 stash 的 message（`obsync-verify-head`）打出来，用户能在
  `git stash list` 里找到自己的改动；
- **别手写 `git stash && pnpm test && git stash pop`**：测试红了 `&&` 链就断，
  改动会留在 stash 里 —— 这正是要写成脚本的理由。

### 实跑验证

在当前（脏的）工作区跑 `pnpm verify:head`：正确报出「HEAD 的测试没有全过」，
pop 后 `git status --porcelain | wc -l` = 27（改动一个不少），`git stash list` = 0 条
（没有残留）。

**未单独验证**：工作区干净时走的那条 else 分支（只打印一行就往下跑测试）——
它与已验的分支共用同一段测试代码，只是跳过 stash。

文档：README 中英的开发一节、HANDOVER 第三节（命令表 + 新增小节）。

### 一个判断上的教训

上一轮我列「你可以定的两件事」时，把「提交拆分」当成了一个可以交给用户的选项。
但**「让 HEAD 保持绿的」这件事不该是选项** —— 它是仓库的基本健康度，该由我主动查
并给出机制，而不是等用户拍板。用户这一问（「为什么你不能帮我做」）提醒的正是这个：
**把「要不要做」和「怎么做」分开 —— 前者可以问，后者不该推回去。**

### 最终处理（用户追问「为什么要我选」之后）

用户的批评更到位：我上一轮那三个选项**全是让他动手**，而且「给步骤，我自己来」
摆在中间，他下意识就顺着选了。**HEAD 是红的属于仓库缺陷，不是偏好，本来就不该
有「先不动」这个选项。** 于是直接做，两个提交：

| 提交 | 内容 |
| --- | --- |
| `353eddf` | `fix(settings)`：补上 d939df2 漏的 `settingsTab.ts`（1 文件 +14 行） |
| `cd4fcb4` | `chore(verify)`：`pnpm verify:head` 脚本 + README / HANDOVER 文档 |

切 hunk 用 `printf 'n\ny\nn\nn\n' | git add -p <file>`（非交互喂答案），每步都用
`git diff --cached` 核对暂存内容。**注意 `settingsTab.ts` 实际有 4 个 hunk 不是 3 个**
（漏看了策略下拉 `commit(true)` 那个）—— 喂答案前先数清楚，答案个数错了会静默错位。

验证：`pnpm verify:head` 跑了两次（每个提交后一次），都是 `Test Files 55 passed (55)`。

**停在这里的部分**：工作区还剩 25 个文件（3 轮未提交改动）。没接着提的理由不是
「该用户做」，而是**它们没有丢失风险** —— 都在工作区里，不像那 14 行曾经只存在于
工作区。要提的话建议合并成 **2 个**（视图改造 / 本轮），不按之前说的 3 个拆：
第 1、2 轮在 `SourceControlView.ts` 和 `styles.css` 里交织在同一块连续新增里，
`git add -p` 拆不开，硬拆要手工编辑 hunk。

### 两条关于「给选项」的
1. **「要不要做」可以问，「怎么做」不该推回去**；而**缺陷类的事（HEAD 是红的）
   两样都不该问** —— 直接修完再报告。把执行成本也做成选项，用户会下意识选
   「省事」的那个，然后问题就留在原地。
2. **选项本身会引导答案。** 「我来补 / 给步骤我自己来 / 先不动」这种三选一，
   中间那个读起来最「不麻烦你」—— 用户顺着选之后，真正该发生的事（修 HEAD）
   反而没发生。**要么别问，要么把选项写成「谁来做」之外的东西。**

---

## 收尾时查出：`d939df2` 漏提交了 `src/settingsTab.ts`（HEAD 是红的）

用户说「继续」，我按惯例先看仓库状态，结果查出**一个真问题**。

### 怎么发现的

`git diff src/settingsTab.ts` 里有一段 `statusBarFullWidth` 的新增，而
`d939df2` 的标题正是「状态栏『占满整屏宽』做成『通用』标签页里的开关」——
**已提交的提交里怎么还有它的改动？** 顺着查：

```
git show --stat d939df2        → 11 个文件，含 tests/features/settingsTabRender.test.ts (+79)
git show HEAD:src/settingsTab.ts | grep statusBarFullWidth  → 空
```

也就是那次提交**把测试和 i18n 键都提交了，唯独漏了设置页那段代码**。

### 实跑确认（不是推断）

```
git stash push -- src/settingsTab.ts
pnpm exec vitest run tests/features/settingsTabRender.test.ts
```

红 3 条，其中一条把原因报得极清楚：

```
expected [ '界面语言', '通用', '显示操作结果提示', '输出调试日志' ] to include '状态栏占满整屏宽'
```

再 stash 全部改动跑**全量**：`Test Files 1 failed | 54 passed (55)` ——
**只有这一个文件红，别处都自洽**。两次都 `stash pop` 恢复了工作区。

### 为什么它能潜伏这么久

**本地跑测试读的是工作区文件（完整的），HEAD 缺什么根本看不出来。**
只有别人克隆、或在 CI 上跑才会撞上。这个项目的 `pnpm test` 一直是本地跑，
所以「工作区全绿」和「HEAD 全绿」被当成了一回事 —— 而它们不是。

**教训**：提交之后要验的是 **HEAD**，不是工作区。一条命令就够：
`git stash && pnpm test && git stash pop`（或用 `git worktree` 检出一份干净的）。
尤其当一次改动**横跨多个文件**（代码 + 测试 + i18n）时，
`git add` 漏掉其中一个是最容易犯的错 —— 而症状恰好是「本地一切正常」。

### 当前工作区状态（留给下一次）

未提交的有 **3 轮**改动，都叠在一起：

| 轮次 | 主要文件 |
| --- | --- |
| 视图改名「仓库同步」+ 顶部并一行 | `SourceControlView.ts`、`main.ts`、`statusBar.ts`、`syncService.ts`、`syncState.ts`、`changeRows.ts`、`types.ts` 及各自测试 |
| 工具条顺序 + 体积两栏 | 同上（`SourceControlView.ts`、`styles.css`） |
| 本轮：注意事项 + reset 挂起 + gitignore | `settingsTab.ts`、`automatics.ts`、`sync/index.ts`、两个 locale、`styles.css` |

另外 `src/settingsTab.ts` 里还压着**两处更早的未提交改动**：
`↑${updates}` → `${updates}`（徽标去箭头），以及 d939df2 漏掉的那段。
提交时要按轮次拆开，不能整个 `git add`。

本地还**领先 `origin/main` 15 个提交**（未推送）。
