# 2026-09-16

## OBSync 项目启动（阶段一完成）

**目标**：单个 Obsidian 插件，合并两个能力 —— 笔记仓库同步（复刻 `obsidian-git`）+ 社区插件安装（复刻 `obsidian42-brat`），并从「仅 GitHub + 英文」扩展到「GitHub / Gitee 双平台 + 中文优先」。

参考项目在本机的位置：
- `F:\_Workspace\GitHub-Project\obsidian-git`（~16.9k 行 TS）
- `F:\_Workspace\GitHub-Project\obsidian42-brat`（~5k 行 TS）

### 已确认的四项决策
仅桌面（不做 isomorphic-git） / 分阶段逐段验收 / id = `obsync` / 加做 GitHub→Gitee 镜像自动发现。

### 阶段一产出
- 脚手架：TS + esbuild（构建即部署到测试库）+ vitest + pnpm。
- `core/`：i18n（中文为规范源）、settings（递归合并默认值，新增设置项免写迁移）、secretStore（SecretStorage 优先 / localStorage 回退）、logger、notice（错误类型 → 用户可读文案）。
- `host/`：`IRepoHost` 接口 + `GitHubHost` + `GiteeHost` + `repoRef` 解析器 + `http` 封装 + `statusMapper`。
- 文档：`docs/reference-analysis.md`（两个参考项目的分析）、`docs/PLAN.md`（复刻规划与阶段划分）。

### 实测发现（读文档看不出来，都是打真实接口才发现的）
1. **Gitee releases 默认升序**。不传 `direction` 时 `mindspore/mindspore` 首个返回 `v0.1.0-alpha`(2020)，传 `direction=desc` 才是 `v2.7.2`(2026)。直接照搬 BRAT 的 GitHub 逻辑会静默安装六年前的版本。
2. **Gitee 的 API raw 端点对匿名请求返回 401**（"登录失效，无权限访问该资源"），**即使公开仓库也如此**。匿名读文件必须走网页通道 `https://gitee.com/{o}/{r}/raw/{ref}/{path}`（302 → raw.giteeusercontent.com）。代价是必须显式给出 ref。
3. **Gitee 的 `html_url` 带 `.git` 后缀**，GitHub 不带。
4. Gitee 鉴权走 `access_token` **查询参数**，GitHub 走 `Authorization` 请求头 —— 这个差异无法用统一装饰器消除。
5. Obsidian `requestUrl` 的 `throw` 选项**默认 true**（400+ 直接抛异常），要自己处理状态码必须显式传 `throw: false`。
6. Gitee 的 `/v5/search/repositories` 匿名调用返回空数组，搜索功能在 Gitee 侧不可用。

### 验证
- `pnpm test`：78 项单测通过。
- `pnpm test:live`：11 项真实 API 测试通过（含一条对照实验，实证 Gitee 默认升序）。
- `pnpm build`：产物 35KB，仅 `obsidian` 一个外部依赖，无 node 内置模块被打包。

### 顺带发现
测试库里已装的第三方插件 `gitee-sync-plus` **不是真 git 实现** —— 只调 `api/v5` + `access_token` 做文件级收发，没有提交历史、分支、冲突处理。所以走真 git 是差异化，不是重复劳动。

### 下一步（阶段二）
插件安装器：安装链路（校验 manifest → release 资产 / raw 回退 → 写入前备份 → 写盘 → 启用）、双列表模型、更新检查、GitHub 社区插件列表浏览、Gitee 镜像发现、设置页与命令。

---

## 接力会话一（同日，ZCode 接手）：阶段二完成

交接文档从本会话起落在 `docs/HANDOVER.md`（必读，含代码地图、实测发现、阶段三开工指引）。

### 阶段二产出（提交 `46170ef`）
- 安装主链路：解析 → 镜像发现（manifest id 二次校验）→ 三级目标回退
  （正式版 → 预发布 → 源码）→ 逐文件回退 → 兼容性检查（写盘前）→
  备份 → 写盘（失败自动回滚）→ 启用/重载 → 记录。
- 单一跟踪列表（`TrackedPlugin`）+ settings 加载时结构校验（坏条目丢弃）。
- 更新检查与执行分离：只提示不自动装；冻结项跳过。
- 社区插件索引浏览（GitHub 独有）、Gitee 镜像发现默认关闭
  （实测 40 个社区插件同 owner 同名命中 0，证据在 `mirrorFinder.ts` 注释）。
- API 限流/不可用降级源码通道并告知用户。
- giteeHost readFile 去掉默认分支预查：网页 raw 通道实测支持 `HEAD`，省一次 API 调用。
- 命令：添加仓库 / 检查更新 / 更新全部；设置页含已跟踪列表（检查/更新/重装/冻结/移除）。

### 本会话踩坑（已写进 HANDOVER.md 第六节与 MEMORY.md）
1. **stub 的 arrayBuffer**：处理器只给 text 时必须派生 arrayBuffer，
   否则资产下载静默为空 —— 曾造成 4 个安装测试假失败。
2. **路由式 mock 给可选文件补 404 路由**，否则重试退避白耗 1.6s/用例。
3. live 测试环境：Gitee 匿名 API 403 限流（3 例）、GitHub 资产 CDN
   （objects.githubusercontent.com）本机网络超时（1 例）—— 均环境问题非回归。

### 验证
- `pnpm typecheck` / `pnpm test`（138 项，1.5s）/ `pnpm build`（部署成功）全绿。
- `pnpm test:live`：8/12 过，4 失败均为上述环境问题（其中 HEAD raw 通道、
  零配额探测 2 个新用例通过，验证了 giteeHost 重构）。

### 下一步（阶段三）
Git 同步。第一件事是实测 Gitee 私有仓库的 `http.extraheader` 鉴权（PLAN.md 风险表第一条）。
开工指引见 `docs/HANDOVER.md` 第七节。

---

## 接力会话一（同日）：阶段三完成

### 产出（同步模块 src/features/sync/）
- `gitManager` 抽象接口 + `simpleGitManager` 实现（状态映射、错误收口、
  pull 三态：merge / rebase / **reset = stash 保护 + hard reset**）。
  刻意不用 obsidian-git 的 update-ref + 普通 reset —— 那会让工作区与 HEAD
  脱节，下次提交把远端改动倒推回去。
- `auth.ts`：http.extraheader 经 simple-git 的 `config`（字符串数组）注入每条命令，
  不落盘；实例按「远端 URL + gitPath」缓存。
- `syncService`：promise 串行队列；sync = 提交→拉取→（再提交）→推送；
  **拉取冲突时链路立即停止**（SyncOutcome 新增 kind:"conflict"），
  在库根目录写《OBSync 冲突指南.md》。
- `automatics`：剩余时间模型定时器（时间戳存 localStorage，重启续表）。
- 状态栏（service 显式驱动）、源码控制视图、编辑远端弹窗、
  8 个命令、设置页（策略三态下拉 + gitPath）。
- esbuild 产物 190KB：simple-git + node 内置（child_process/fs/os/path）
  全部走 external，Electron 运行时可用 —— 与 obsidian-git 同构。

### simple-git 实测坑（详见 HANDOVER.md 第六节）
1. `config` 是字符串数组不是对象；`simpleGit()` 收 Partial<SimpleGitOptions>。
2. `git.log({max})` 错，要 `maxCount`（--max-count）。
3. 空仓库 commit 不抛错，返回 changes=0 摘要。
4. HEAD 未出生时 restore --staged 报错，用 `git rm --cached` 回退。
5. 无提交的仓库 `git branch` 输出为空。
6. push 不更新裸仓库 HEAD，测试环境要手动 symbolic-ref。
7. 测试机全局 autocrlf=true，内容断言要归一 \r\n。

### 测试
- simpleGitManager：**真实临时 git 仓库**驱动（mkdtemp + 系统 git），
  15 项覆盖 init/状态/提交/分支/推送/拉取/冲突/abortMerge/push 拒绝。
  提交身份用 addConfig 设仓库本地，不碰全局配置。
- syncService：可编程假 GitManager 验证编排（顺序/冲突停止/串行化，
  用带延迟的 status 计数直接观察 maxInFlight=1）。
- 全套 174 项单测绿（~30s，含真实 git 用例）；build 部署成功。

### 待办（阶段三收尾 → 阶段四）
- **唯一风险项**：对真实 Gitee 私有仓库实测 extraheader push（需用户令牌）；
  失败回退 askpass（obsidian-git simpleGit.ts:249 的做法）。
- 测试库手动冒烟：装插件、配令牌、同步真实仓库。
- 阶段四：错误文案统一、README、发版流程。

## 接力会话一补记：启动报错修复

用户实测启动报 `TypeError: t.app.workspace.addStatusBarItem is not a function`。
根因：`addStatusBarItem()` 是 **Plugin 类**的方法（obsidian.d.ts:4947），
不在 `Workspace` 上 —— 我在 statusBar.ts 里猜错了归属对象，且 stub 里
没有 workspace 这种真实对象差异，单测拦不住。

修复：状态栏元素由主类 `this.addStatusBarItem()` 创建后经 `SyncDeps.createStatusBarItem`
传入 StatusBar，模块不再触碰 workspace。已记入 HANDOVER.md 第七节第 8 条。
174 项测试绿，重新部署完成。

## 接力会话一补记 2:新功能「绑定已有插件」(提交 1a73339)

用户需求:省去手动逐个添加已装插件的功夫。
方案:扫描 {configDir}/plugins/(以磁盘为准,能发现刚手动拷入的插件)→
官方社区索引按插件 id 反查来源仓库(manifest 规范没有 repo 字段,
community-plugins.json 是唯一权威映射)→ 弹窗勾选绑定(只写跟踪列表)。
索引外的插件(大量中文/Gitee 插件)标「来源未识别」留手动添加 ——
数据源边界,已在弹窗里向用户说明。
测试 179 项全绿;fakeApp.adapter 补了 list()。

## 接力会话一补记 3:可用更新常驻徽标(提交 e9a0729)

用户反馈:检查全部更新只弹 Notice 容易错过。
方案:检查结果持久化进 installer.availableUpdates,列表行渲染
「可更新 → 版本」徽标 + 整行高亮(CSS .obsync-has-update)。
写入:checkAll(含启动检查)/单插件检查;检查失败保留旧记录。
清除:安装成功(recordInstalled)/不在跟踪列表(normalizeSettings 剪枝)。
顺带修了 mergeWithDefaults 的通用缺陷:空对象默认值(映射表形状)
会在合并时被整体丢弃 —— 现改为透传给下游 sanitize。
测试 181 项全绿。

## 接力会话一补记 4:目录名 ≠ manifest id(提交 3c4e660)

用户报「好几个插件未识别来源,明明都上了官方市场,比如 MDRazor」。
真实数据诊断(读测试库 32 个插件 + 拉线上索引比对):
- 5 个插件目录名与 manifest id 错位:MDRazor/→md-razor、
  obsidian-commander/→cmdr、obsidian-pkmer/→pkmer、
  obsidian-plugin-manager/→plugin-manager、obsidian-style-tuner/→style-tuner。
  成因:手动解压 release 或别的安装器用仓库名建目录。
- 按目录名查索引 19 命中,按 manifest id 查 24 命中 —— 差的就是这 5 个。
- md-razor 确实在官方索引里(repo dyse-sofqi/MDRazor),用户判断正确。

修复:
1. 绑定识别改用 manifest.id(去重、排除 obsync 自身)。
2. 新增 pluginFolder.resolvePluginFolder:按 manifest id 反查真实目录,
   文件读写删/loadManifest 全走它;找不到时回落 plugins/{id}(全新安装)。
   否则更新 MDRazor 会新建第二份同 id 安装。
3. loadManifest 要目录、enablePluginAndSave 要 id —— 不再混用。

剩余 8 个未识别确实不在官方索引(pkmer/trefoil/bewater/qimen/lyricflux/
Enhanced-editing 等,PKMer 等非官方渠道分发;obsidian-regex-replace 是旧 id 残留)。
测试 186 项全绿,已记入 HANDOVER.md 第七节第 9 条。

## 接力会话一补记 5:检查时机调整 + 冻结文案修正(提交 c5bc825)

用户要求(明确不做自动更新):
1. 启动检查默认关闭 —— autoCheckOnStartup: true → false,
   SETTINGS_VERSION 1→2,迁移把 v1 数据里持久化的旧默认 true 一并纠正
   (v1 用户多半从没动过该开关,不纠正则新默认形同虚设);v2 后尊重用户选择。
   启动延迟项在开关关闭时置灰。
2. 冻结文案「不参与自动更新」→「不参与更新检查」(产品没有自动更新)。
3. 新增 installer.autoCheckOnSettingsOpen(默认开):打开设置页即查一次,
   结果落徽标;用 display/hide 区分「打开页签」与「页内重绘」,加 10 分钟
   节流(installer.lastUpdateCheckAt 持久化,由 recordUpdateChecks 刷新);
   自动检查静默 —— 全部最新时不弹提示,有更新靠徽标呈现。
   判定逻辑抽成纯函数 shouldCheckOnSettingsOpen 以便单测(node 环境测不了 UI 时序)。

测试 193 项全绿。

## 接力会话一补记 6:设置页拆四个标签页(提交 ce126b9)

用户要求的信息架构调整:
1. 已追踪插件(第一页):三个按钮(添加仓库/绑定已有/检查全部)+ 跟踪列表。
2. 插件安装器:原设置项 + 访问令牌段(从独立段落移入)。
3. 仓库同步:原「笔记同步」更名(标签 + 段标题)。
4. 通用:界面语言 + 提示/日志开关。

实现:Obsidian 的 PluginSettingTab 无内建分页 → 自绘 .obsync-tabs 标签栏,
activeTab 存内存(重绘/切页不回弹),切标签不重复触发设置页自动检查。
顺带记录一个验证小坑:产物里的中文是 esbuild 转义的 **大写** 十六进制
(\u5DF2...),用小写去 grep 会误判成"文案丢失"。
测试 193 项全绿。

## 接力会话一补记 7:设置页视觉打磨(提交 6643d7a)

用户要求「简单美化设置页」。做法(不改信息架构):
- 状态信息改徽标药丸:可更新(强调色)/已冻结/令牌已配置,描述行只留事实。
- 可更新的列表行:左侧强调条 + 淡底色 + 名称着色。
- 「已追踪插件」标签挂计数(跟踪数 + 有更新时强调色 ↑N)。
- 三个主操作按钮独立成卡片;令牌状态药丸随输入/测试/清除即时更新。
- 提示列表(社区插件浏览/版本选择)小字号元素改块级堆叠。

两个值得记住的点:
1. `.setting-item-*` 是 Obsidian **全局**类 —— 样式必须限定在自加的
   `.obsync-settings` 容器类下,否则污染其他插件的设置页。
2. 自查手段:npx esbuild styles.css 验语法 + 脚本比对「代码用到的类」
   与「CSS 定义的类」(本次据此发现 3 个漏样式的类)。
   注意 .probe 已在 gitignore,脚本不随仓库走。

## 接力会话一补记 8:按用户截图反馈调整(提交 2c4658b)

用户标出三处:操作卡片丑、列表图标是否垂直居中、三个动作图标辨识度低。
1. 空卡片 → 头部栏:标题+说明+三个按钮同行(左文右按钮),列表不再重复标题。
2. 图标:检查=search(原 refresh-cw)、更新=download(有更新时强调色实心)、
   重装=refresh-cw(原 rotate-cw)。根因是两个圆箭头(refresh-cw/rotate-cw)
   视觉上几乎一样。ExtraButtonComponent 无 setClass,用 extraSettingsEl 挂类。
3. 垂直居中:Obsidian 默认让控件贴顶(截图中图标对齐名称行),
   显式 align-items: center 修正。

经验:IconName 只是 string,编译期不校验图标名 → 选图标要靠
「截图里已在渲染的名字」+ Lucide 核心老图标(如 search)来保证存在性。

## 验收复查（墨回来接手）：修 2 个真 bug + 补 3 处降级 + 补回 1 个漏做的功能

用户把项目交给 ZCode 跑完阶段二、三后，让我独立验收。做法是**不看实现者的结论，
只跑验证 + 读代码**。结论：架构与文档质量确实好（无 `as any`、无 ts-ignore、
无 console 泄漏、无 TODO），但**测试套件实际是红的**（10 个失败），
而且有几处「文档说做了、实际没生效」。

### 真 bug

1. **`unstage` 的「HEAD 未出生」回退从未触发**。正则写的是
   `/could not resolve HEAD/`，而 git 实际输出是 `fatal: could not resolve 'HEAD'`
   —— **带单引号**。回退分支形同虚设，症状是「全新仓库里取消暂存直接报错」。
   HANDOVER 第六节第 4 条还写着"unstage 已做回退"，属于文档比实现乐观。
   教训：**靠 git 的错误文案做分支判断，正则必须用真实输出校准**，
   不能凭记忆写（我记忆里也是"没引号的版本"）。

2. **`registerView` 用了 `this.sync!` 且无平台守卫**。`createSyncModule` 在移动端
   返回 undefined，视图工厂会解引用它。命令入口有守卫，但视图类型一旦注册，
   恢复工作区布局时仍可能被实例化。

### 真缺口（都影响用户，不是洁癖）

3. **release 资产下载失败 = 整个安装失败**。同一个文件在源码通道现成可取，
   却因为资产 CDN 抖动而整体失败。实测资产地址（`github.com/.../releases/download/`
   → `objects.githubusercontent.com`）**3 次里 2 次 21 秒超时 0 字节**，
   而 `api.github.com` / `raw.githubusercontent.com` 稳定。
   → `loadReleaseFile`：资产下载**失败**（不只是"不存在"）也回退该 tag 的源码。

4. **GitHub 的 raw 域名不可达时没有退路**。国内 `raw.githubusercontent.com` 常被阻断
   而 `api.github.com` 可达，两者可达性互不相关。Gitee 侧早就有 API↔网页 的双通道，
   GitHub 侧只有一条路。→ `readFile` 回退 contents API。

5. **`fileWebUrl` / `commitWebUrl` 是死代码**。阶段一就把这两个跨平台 URL 构造器
   写好了（还从 hostRegistry 转出），但没有任何地方用 ——
   也就是 PLAN 里规划的「在浏览器打开文件/历史」（`openInGitHub` 的 provider 化）
   根本没接上。→ 新增 `features/sync/remoteLinks.ts` + 2 条命令 + 文件右键菜单。
   两个平台网页路径格式一致，所以**没有平台分支**。

### 测试质量

6. **live 测试长期飘红**：Gitee 匿名 API 限流导致 4 个用例失败，文档的说法是
   "等窗口重置再跑"。但**预期会红的套件比跳过更糟** —— 它会训练人忽略失败，
   真有回归时也看不出来。→ 限流时跳过并打印可行动提示（配 `OBSYNC_GITEE_TOKEN`）；
   配了令牌还被限流才真失败。走网页 raw 通道的用例**不**包装（那条不吃 API 配额）。
7. **9 个 git 用例超时**：根因是本机**进程创建约 340ms**（连 `cmd /c echo` 也要 317ms，
   不是 git 特有的）。`makeCluster()` 一个用例起十几次 git 就要 4~5 秒，
   vitest 默认 5 秒必然不够。→ `testTimeout: 30_000`，清理加 `maxRetries`（Windows EBUSY）。
   **`simpleGitManager.test.ts` 单独跑约 150 秒** —— 文档早先写的"整个套件 ~1.5s"是错的。

### 顺带验证到的

- **`http.extraheader` 的客户端侧行为已实证**：用本地 HTTP 服务器验证 git 在
  **第一个请求**（`/info/refs?service=git-upload-pack`）就带上 `Authorization: Basic`，
  不等 401 挑战；再用「让 git 在同一次调用里读回 `-c` 配置」验证 simple-git 的
  `config` → `-c` 传递。剩下未验证的只有 **Gitee 服务端是否接受令牌当密码**。
- **simple-git 3.36 起会守卫 `.env()` 传入的环境变量**：本机有 `GIT_PAGER=cat`，
  传 `{...process.env}` 会直接抛 `Use of "GIT_PAGER" is not permitted without
  enabling allowUnsafePager`。表现为「本地服务器收到 0 个请求」，看着像网络问题。
  生产代码不调 `.env()`，不受影响 —— 这是测试写法的坑。

### 验证结果
- `pnpm test`：**208 项全绿**（19 个文件，145 秒）
- `pnpm test:live`：GitHub 全过；Gitee 的 3 个 API 用例因限流跳过（符合预期）
- `pnpm build`：通过，产物 203KB

### 剩下的待实测项（只有一项）
对真实 Gitee 私有仓库 push 一次，确认服务端接受 `http.extraheader` 的 Basic 认证。
需要用户的令牌与私有仓库。失败的回退方案是 askpass 弹窗（obsidian-git 的做法）。

## 验收复查续：i18n 缺口（同日晚）

上一轮修完逻辑层后，继续查 UI 层与 i18n。**发现一个系统性缺口**：

### 问题
i18n 的编译期保证只能覆盖「locale 文件之间的结构一致」，
**管不住「代码里直接写了一句中文」**。而错误路径恰恰最容易这么写 ——
因为抛出点（`manifest.ts` / `pluginFiles.ts` / `pluginFolder.ts` /
`simpleGitManager.ts`）都是**纯逻辑，拿不到 `t`**。

写了个扫描脚本（`.probe/check_hardcoded_cjk.py`）一量：**26 处**硬编码中文。
其中 22 处是用户可见的错误文案 —— 也就是**英文界面下会冒出中文错误**。

佐证：i18n 里有 **7 个键写了却从未被引用**
（`gitNotFound` / `missingManifest` / `missingMainJs` / `noReleaseFallback` /
`sourceRelease` / `sourceRaw` / `installing`）—— 正是「本来打算本地化但没接上」的证据。

### 修法
沿用本轮为 sync 建立、并已验证的模式：

| 层 | 职责 |
| --- | --- |
| 逻辑层 | 抛 `InstallerError({ kind, ...params })`；`message` 只放**技术性描述**（英文，进日志） |
| 注册 | `createXxxModule()` 里 `notifier.registerErrorTranslator(...)` |
| 展示层 | `Notifier.describeError()` 按类型码取 locale 文案 |

关键设计：**可辨识联合 + `switch` 穷尽检查**。新增类型码却忘了加文案，
`const exhaustive: never = detail` 会**编译报错** —— 漏翻译不会留到运行时。
`Notifier` 用**注册制**而非 `import` 各功能的错误类型，`core/` 保持对 `features/` 无知。

顺带发现**两处错误类型用错**：「没有上游分支」「游离 HEAD」都抛了 `GitNotRepoError`，
提示语是「请先初始化仓库」—— 让用户去初始化一个已存在的仓库，**指错方向**。
新增 `NoUpstreamError` / `DetachedHeadError`。
（教训：**错误类型用错比没有类型更糟**。）

### 结果
- 硬编码中文 26 → **4**，剩下 4 处都是合理的：
  语言下拉的「简体中文」标签（本就该用母语写）、`giteeHost` 的三个限流**检测词**。
- 测试约定改为**断言类型码，不断言消息文本**（文案来自 locale，改文案不该让测试变红），
  助手 `tests/helpers/expectInstallerError.ts`。

### 另外两个自动化自查脚本（`.probe/`，已 gitignore）
- `check_css_classes.py`：比对代码里用到的 `obsync-*` 类与 `styles.css` 里定义的类。
  当前 20 用 20 定义，无遗漏无僵尸。
- `check_hardcoded_cjk.py`：上面那个。**新增的中文都该是可疑的。**

### 顺带确认没问题的
- 设置页四个标签页的逻辑（`display`/`hide` 区分「打开」与「重绘」、10 分钟节流）正确，
  不会因为 `checkAllUpdates` 的 `finally { display() }` 造成递归。
- `manifest.json` / `versions.json` 字段正确（`isDesktopOnly: false` 是对的 ——
  安装器在移动端可用，同步命令在桌面端才注册）。

### 同轮顺带修的小问题
- **安装器的命令没有插件名前缀**：同步命令叫「OBSync：立即同步」，安装器命令却直接用了
  弹窗标题（「添加插件仓库」）。Obsidian 用户按插件名搜命令，没前缀就搜不到 ——
  命令面板里会散落一串认不出归属的条目。新增 5 个命令专用键；
  弹窗标题保持不带前缀（用户已经在弹窗里了，不需要）。
- 设置页术语混用：「已追**踪**插件」（标签）vs「已跟**踪**的插件」（同页标题）→ 统一为「跟踪」。
- `autoCheckDelay` 的置灰状态不随开关更新（`commit()` 不重绘）→ 持有 `TextComponent`
  引用即时 `setDisabled`。
- **补了 `README.md`**（中文优先）—— 这是阶段四发布件里唯一缺的东西。
  写的时候顺手核对了命令名，才发现上面那个前缀问题。

### 阶段四还差什么
- 发版流程（GitHub Release + 上传 main.js / manifest.json / styles.css）
- 上架官方市场的材料（如果要做）

## 补上装配路径的测试覆盖（同日晚）

`main.ts` 的 `onload()` 此前**没有任何测试覆盖** —— 而它恰恰踩过坑：
曾经把状态栏元素挂到 `app.workspace.addStatusBarItem()`（真实 API 在 `Plugin` 类上），
**单测全绿、真机启动才 TypeError**。

做法：把 stub 的 `Plugin` 基类补全（`addStatusBarItem` / `registerView` /
`registerEvent`），给假 app 补最小 `workspace`（`onLayoutReady` / `file-menu` 等）
和 `adapter.getBasePath()`（`getVaultRoot` 会调它，缺了在 `onload` 里就炸），
然后让真实的 `onload()` 跑在假 app 上。新增 `tests/pluginBoot.test.ts`（15 项）：

- 桌面端：两模块都装配上、注册了设置页/侧栏图标/**状态栏元素**、注册了视图、
  命令名都带 `OBSync` 前缀、命令 id 不重复、`onunload` 不抛错
- 移动端：`sync` 为 `undefined`、不注册视图与同步命令、不创建状态栏元素

**做了变异验证**：把 `createStatusBarItem` 改回 `app.workspace.addStatusBarItem()`，
测试立刻以 `TypeError: ... is not a function` 失败 —— 确认它真能拦住这类问题，
不是"永远通过"的装饰。

顺带发现：`getVaultRoot` 依赖 `adapter.getBasePath()`，这是个**只在装配时才会调到**
的依赖 —— 之前没人测过这条路径，所以没人发现假 app 缺它。

局限（写进了交接文档）：它不能替代真机验证（渲染、真实网络、Obsidian 内部 API
的实际行为仍要手测），但能把「装配写错」从"真机才发现"提前到"跑测试就发现"。

## 自动定时器的两个真 bug（同日晚）

继续查定时器 / 串行队列 / 视图生命周期 —— 这几类最容易出隐蔽 bug。在
`automatics.ts` 里发现两个，都是"读代码不容易看出来"的那种：

### 1. 时间戳没按库隔离
用原生 `globalThis.localStorage` 存「上次执行时间」，而那是**所有库共用的存储区**。
于是 A 库刚自动提交过，会让 B 库刚打开就立刻提交一次；两个库的自动周期互相干扰。

**对照参考项目确认过**：obsidian-git 用的是 `app.saveLocalStorage` / `loadLocalStorage`
（按库 API），而且专门写了一段迁移把老数据从原生 localStorage 搬过来 ——
说明这是个它已经修过的问题。OBSync 漏了这一条。
→ 改走 `app.saveLocalStorage` / `app.loadLocalStorage`。

### 2. `stop()` 挡不住 in-flight 的 `fire()` 重新起表
`fire()` 是异步的，跑完会重新起表。若这期间 `stop()` 被调用过（插件卸载、
设置变更触发的 `restart()`），那个 in-flight 的 `fire()` 回来照样起表：
- `stop()` 之后仍有定时器在跑；
- **更糟**：`restart()` 已经起过一个，新起的会把 `Map` 里的记录**覆盖掉**，
  先前那个**再也 clear 不掉** → 同一动作每个周期跑两次。

→ 引入世代计数器：`stop()` 自增，`fire()` 开始时捕获、回来时若已变则放弃起表。

### 验证：两次变异验证
计时类代码"看起来对"很容易，所以两个修复都做了变异验证：
- 去掉世代守卫 → 两条竞态用例失败（`stop()` 后仍在跑，得到 4 次调用而非 1 次；
  `restart()` 场景 3 次而非 2 次，**正是预测的重复触发**）
- 改回全局 localStorage → 两条存储用例失败

新增 `automatics.test.ts`（8 项，用 `vi.useFakeTimers()`）。
其中一条精确断言了「启动时剩 20 秒 → 恰好在启动后 20 秒触发」，
同时证明了「按剩余时间起表」而不是「重新计满」。

## syncService 里的三个问题（同日晚）

接着查串行队列与编排。发现一个**数据完整性 bug** 和两个语义错误。

### 1. 冲突未解决时「提交全部」会把冲突标记提交进历史（真 bug）
冲突文件在 `git status` 里是 `UU`，于是 `mapStatus` 把它**同时**归进 `staged`
（index 位非空）与 `unstaged`（worktree 位非空）。结果是 `doCommitAll` 里那个

```ts
if (dirty === 0 && status.conflicted.length === 0) return { kind: "nothing-to-commit" };
```

在有冲突时**必然放行** —— 继续 `git add -A` + 提交，把 `<<<<<<<` / `>>>>>>>`
当普通内容写进历史。而本模块的设计文档明说这是要避免的事。

**触发路径很现实**：上次同步遇到冲突没处理 → 自动提交定时器到点 → `sync()`
第一步就是 `doCommitAll()`。

→ `doCommitAll` 开头显式拦冲突并抛 `ConflictError`（复用已有的错误类型与文案链路）。
变异验证：去掉守卫后用例报 `promise resolved "{ kind: 'committed' }"`，
也就是**它真的提交了**。

### 2. `isBusy` 的语义错了
```ts
this.running = true;
return task.finally(() => { this.running = false; });
```
布尔量在**第一个任务 settle 时**就置 false，而此时队列里第二个任务还在跑 ——
于是「忙」在真正有活干时报"空闲"。automatics 靠它决定"跳过本轮"，
状态栏也靠它显示忙碌状态，语义错了会连锁出错。
→ 改用计数器（排队中 + 执行中）。

### 3. `sync()` 整条链路都显示「正在提交」
只在开头 `setActivity("committing")` 一次，拉取与推送阶段状态栏显示的是错的。
→ 按阶段更新。

新增 5 项用例（含变异验证）。`syncService.test.ts` 从 7 项增到 12 项。

### 方法论小结（这轮反复用到）
**变异验证是唯一靠得住的确认方式**。计时/并发/守卫类代码"看起来对"太容易，
每个修复都注入回原 bug 确认用例变红 —— 这轮三次变异都观察到了预期的具体现象
（调用次数翻倍、`resolved` 而非 `rejected`、存储位置不对），而不是笼统的"失败了"。

## 视图层的两个问题（同日晚）

### 1. 点按钮失败时界面毫无反应（真 bug）
`SourceControlView.run()` 的 catch 写着「通知已由 service/notifier 完成」，
但那只对两种情况成立：拉取冲突（`doPull` 里处理）与没有远端（`doPush` 里处理）。
其余错误 —— **推送被拒、鉴权失败、找不到 git、网络问题** —— 会原样上抛，
然后被**静默吞掉**。症状：用户在视图里点「推送」，远端拒绝了，界面上什么都不发生。
→ catch 里调 `notifier.reportError(err)`。

（命令入口 `main.ts` 的 `runSyncAction` 是对的，只有视图这条路漏了。
教训：**同一件事有多个入口时，错误处理容易只做对一处**。）

### 2. 冲突文件在变更列表里显示三次
又是那个 `UU` 根因：冲突文件同时进 `staged` 与 `unstaged`，
列表再单独渲染一轮 `conflicted`，于是一个冲突出现三次。
→ 抽出 `visibleChanges()` 过滤掉 `conflicted` 状态，并加 4 项测试。
（抽成纯函数而不是做 DOM 级测试 —— 视图的 DOM 测试性价比太低。）

### 顺带排查
grep 了所有 UI 文件的裸 `catch {}`，只剩 `safeListBranches`（列分支失败返回空数组，
是有意为之），没有同类问题。

## 「编辑远端」的校验过严（同日晚）

`EditRemoteModal` 用 `tryParseRepoRef` 做校验，等于**只接受 GitHub / Gitee 地址**。
但**同步本身是纯 git 操作** —— 自建 GitLab、内网 git 服务器、本地裸仓库、
`file://` 路径都能同步。被拒的理由说不通，而提示语只说"无法识别该仓库地址"，
用户根本不知道原因（他填的是一个完全合法的 git 远端）。

只有两个能力真的依赖平台识别：**鉴权注入**（`credentialForRemote` 认不出平台就返回
undefined，回退到系统凭据助手）和**「在远端打开」**。

→ 改成宽松校验：只拦明显写错的输入（带空格的句子、中文），
非 GitHub/Gitee 的合法远端**放行但给不阻断的提示**（随输入实时显示），
留空仍然合法（用户想暂时断开同步）。

教训：**校验要区分「技术上必须」和「产品上倾向」**。
前者该硬拦，后者该提示 —— 把后者做成硬拦，会让用户在一个完全可用的场景里卡住。
